<?xml version="1.0" encoding="utf-8"?>
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:inset="8.564815%">
    <!-- The supplied artwork is unchanged; its own margins plus this inset
         keep the R inside the adaptive launcher mask. -->
    <bitmap android:src="@drawable/reply_pilot_artwork"
        android:gravity="fill" android:filter="true" android:antialias="true" />
</inset>
