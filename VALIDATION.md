# Validation record — 0.12.2

## 0.12.2 Opinion replies and Your views — 2026-09-25

- Cause of “Got your message. Let me get back to you.” for an opinion question (“How do you envision the next 4 years with AI… give me a prediction… how will that affect the economy”): the Autopilot instructions told the model to defer on “unsupported expertise” and “general-assistant requests”, so it flagged attention, and the phone replaced any flagged reply with a fixed fallback. Now opinion questions are ordinary replies in the owner's voice, the owner's typed views (`ownerViews`) guide them, and flagged replies keep the model's own safe wording for `plans`, `sensitive` and `uncertain` (`personal_info` and `model_unavailable` still use the fixed deferral; commitments, over-long text and claimed whereabouts still fall back).
- Relay: **162 tests** pass, including new `opinions.test.mjs` (3): the question reaches the model with `ownerViews` and returns as an ordinary reply; instructions include the opinion rules and drop the old deferral wording; `ownerViews` is bounded, trimmed and only sent for Autopilot.
- JVM: **626 tests** pass in the cloud check, including new `OwnerViewsTest` and a new `AutopilotPolicyTest` case for kept wording; main sources compile against SDK 36 stubs with flow analysis (only the unavailable-library files are skipped).
- Browser: new `tools/ui-owner-views.cjs` passes (explanation, unsaved text kept across refreshes, failed save keeps text, exact save and reload, hidden for older builds). `ui-battery`, `ui-train-autopilot`, `ui-autopilot-simple`, `ui-sim-help`, `ui-manual-takeover`, `ui-inbox-touch`, `ui-keyboard` and `ui-text-mms` still pass.
- Follow-up from the Pixel on 0.12.1: “How would you use it” got “Got your message…” and then “Let me get back to you on that.” The model had flagged it `uncertain` and then `personal_info`. Questions about the owner are now ordinary conversation, answered from Your views, the persona and earlier messages (general answer plus an `uncertain` alert when none of these covers it). `personal_info` is reserved for private or checkable facts, and flagged replies of any reason keep the model's wording unless they look like they carry a private detail (3+ digits, a street address, a link, an email, or credential words). New `AutopilotPolicyTest` cases cover kept and blocked `personal_info` replies.
- `Auto Updater.command` was dry-run in the cloud against stand-in scripts, a bundle and a local origin: a first update ran server, build and push; an update announced before its files were saved waited, then ran; a docs-only update skipped the server and build; a failing server update stopped without retrying until a new ready note, then succeeded on retry. It never deletes files.
- Physical Pixel checks remain: an opinion question and a question about the owner from a real contact, with and without Your views filled in.

## 0.12.1 Plan deferrals and locked-phone replies — 2026-09-25

- Cause of the repeated “I'll let you know in a bit.”: every plan message was short-circuited on the phone (and on the relay) to one fixed text without calling the model, so each new push got the same line. Now the phone counts Autopilot's plan deferrals in the chat since the owner's own latest reply (manual send or manual takeover) and within 12 hours: 0 or 1 → the relay writes a deferral in the owner's voice (`planDeferral.count`), 2 or more → no reply, a `plans_need_input` decision and a **Chat needs attention** alert on every further push until the owner replies. Model output that is a commitment, claims whereabouts, is too long or repeats an earlier sent text is replaced by a varied fallback that also skips texts already in the chat.
- Cloud checks (the Mac build is still to run): main sources compile against Android SDK 36 stubs with flow analysis; the only errors are the unavailable okhttp/jsoup/androidx/MMS-library classes in untouched files. **624 JVM unit tests** pass, including new `PlanDeferralPolicyTest` (5); the two tests needing okhttp/Android runtime are left to Gradle on the Mac.
- Relay: **159 tests** pass, including new `plan-deferral.test.mjs` (7): old fixed path unchanged without `planDeferral`, AI deferral with forced `plans` attention, count-1 instructions, unsafe/repeated/whereabouts/long output and provider failure falling back, strict validation, non-plan messages unaffected, untrained chats still gated. No new relay module, so the Dockerfile list is unchanged.
- Browser: new `tools/ui-battery.cjs` passes (Settings **Reply while locked** row, Autopilot-chat note shown only while Autopilot is selected, one-time Android prompt, Allowed state, hidden when an older phone build omits the flag). `ui-train-autopilot`, `ui-autopilot-simple` and `ui-sim-help` still pass.
- Android: new normal permission `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`; the snapshot reports `PowerManager.isIgnoringBatteryOptimizations`; **Allow** opens Android's one-time prompt (falling back to the battery list, then App info). Reply jobs (`DraftJob`, three builders) use `JobInfo.PRIORITY_HIGH` on Android 13+; they are not expedited because they need a minimum latency for burst collection.
- Physical Pixel checks remain: the plan sequence with a real contact (two different deferrals, then silence with alerts), a manual reply resetting it, and Autopilot replies with the screen locked after allowing the exemption.

## 0.12.0 Train Autopilot — 2026-09-25

- Built on the owner's Mac with `Build Reply Pilot.command`: `testReleaseUnitTest` and `assembleRelease` passed, **646 unit tests** with zero failures. APK `dist/Reply-Pilot-0.12.0.apk` (version code 66), SHA-256 `2c9b135a975ef3eaa19d017584fa14167355a68d44068174c41430d579b2bb4e`; `tools/check-apk-signer.py` confirmed the owner's signer `667cf8c2…8bdb`.
- Relay: **152 tests** passed, including `persona.test.mjs` (11). `Update Server.command` deployed to Railway (deployment `d3339394-6f49-4e56-923c-e66fb863995a`) and the service answered afterwards.
- Browser: `ui-train-autopilot` and updated `ui-autopilot-simple` passed with fictional data. `tools/check-personas.py` exercised the encrypted persona store schema.

## 0.11.1 Sending SIM recovery — 2026-09-25

## 0.11.1 Sending SIM recovery — 2026-09-25

- Built on the owner's Mac with `Build Reply Pilot.command` (Java 21, Android SDK 36, Gradle 8.13): `testReleaseUnitTest` and `assembleRelease` passed, **640 unit tests** with zero failures, errors or skips, including 5 new `SimPolicyTest` cases (keep the active saved SIM, recover to the only active SIM, never guess among several SIMs, no-SIM handling, owner-facing messages). `lintVitalRelease` passed during assembly.
- APK `dist/Reply-Pilot-0.11.1.apk` (version code 65), SHA-256 `a5e6e075aff34237d2c82d5de1ed88219d5cf66a859b1060ffa035cb50bb6a40`. `tools/check-apk-signer.py` confirms v2 signer `667cf8c2758f1a7674674c2308385b30e629260c6dd9f5d883b5b03f59ea8bdb`, unchanged from 0.6.x through 0.11.0, so it installs as an update. No permission changes.
- Browser: new `tools/ui-sim-help.cjs` passes with fictional bridge data. It covers an empty menu without Phone access (explanation, Allow Phone access, Open app permissions), recovery selecting the only SIM, immediate save of a SIM choice, and the "No active SIM found" state. `ui-manual-takeover`, `ui-autopilot-simple` and `ui-inbox-touch` still pass. `ui-send`, `ui-send-recovery`, `ui-setup`, `ui-smoke`, `ui-security`, `ui-safety`, `ui-text-mms` and `ui-attachments` fail identically on unmodified 0.11.0 in the headless Chromium used here (touch-hold timing and fixture timeouts), so those failures are environmental and not caused by this change.
- Physical Pixel checks remain: install over 0.11.0, confirm Settings → Sending SIM lists the SIM, and send a manual text. If the menu says "No active SIM found" while Phone access is allowed, Android itself isn't reporting an active subscription.


## 0.11.0 Autopilot simplification and initial history analysis — 2026-09-25

- Final release build passed: **635 unit tests**, zero failures/errors/skips; lint has zero errors and11 pre-existing warnings. Signed release assembly succeeded.
- Installer verification passed for0.11.0/code64: unchanged signer/permissions, byte-matching bundled assets and Tiny Blast audio, disabled backup/debug/cleartext, protected private components,16 KiB alignment and known-credential exclusion. Evidence: `dist/installer-verification-0.11.0.json`. APK SHA-256: `0bde351b101b3f717883ed9856b07f968bd9bfd99f372f2b0b42fe090da67c94`.
- Relay:141 tests passed, including the HTTP boundary. New fixtures cover ordinary acknowledgment replies, planning/unknown-fact deferrals, strict attention metadata, model-error fallback, history auth/bounds, memory response schema, failed-batch retries and successful request deduplication. Existing client tests continue to pass. All model responses were mocked.
- Browser: `ui-autopilot-simple`, `ui-manual-takeover`, `ui-send`, `ui-send-recovery` and `ui-inbox-touch` passed with fictional conversations. Checked only Off/Autopilot, Instant/1m/5m, eligibility warning removal, active history progress/polling, stale-response and revoked-access handling, manual typing/sending, receipts and navigation. Pixel-sized412×915 setup/history screenshots were inspected without clipping or overlapping controls.
- SQLite: `check-autopilot-migration`, `check-history-learning`, `check-history-archive`, `check-manual-takeover`, `check-text-mms-storage`, `check-mms-downloads` and `check-mms-outgoing` and `check-contact-guidance` passed. These exercise migration preservation, encrypted checkpoint atomicity/resume, long-message fragment accounting, contact isolation, incomplete-text rejection, WAL progress, archived pagination, manual-send identity and carrier callback/source bindings. They do not simulate Android's real carrier/provider lifecycle.
- A transient MMS receipt now defers the exact live SMS generation job instead of saving a permanent no-reply decision. Foreground review suggestions do not compete with Autopilot; explicit owner-requested drafts remain available. Try AI uses the current Autopilot contract with no real recipient or send permission.
- Initial history analysis is a separate encrypted phone checkpoint database. A completed flag requires all available eligible text in the initial snapshot to be processed; unreadable/truncated MMS text causes an explicit retryable error. Long text is fragmented without silent truncation. Network/rate-limit failures preserve progress; corrupt checkpoints rebuild. Native access, connection, recipient, generation and manual-takeover checks guard publication and sending.
- Hosted deployment `8628410b-a9e6-4e16-9470-2ec07c0645a9` completed. Authenticated production health accepted the existing phone credential and advertised `autopilotVersion:1`, `historyAnalysisVersion:1`, the unchanged `gpt-6-sol` model and existing200/day limit. No live model generation, real message-history upload or SMS/MMS send was performed during validation.
- Physical Pixel tests remain outstanding: first full-history upload and progress under real phone storage/network conditions, exact alarms/background execution, carrier SMS/MMS, notification sound/lock screen and model response quality. Initial learning can consume the shared daily quota; it reports an error and preserves progress instead of treating partial learning as complete. Android/carrier latency still applies to Instant mode.

## 0.10.4 Manual sending takes precedence — 2026-09-25

- `testReleaseUnitTest`, `lintRelease` and `assembleRelease` passed: **607 unit tests**, zero failures/errors/skips; lint zero errors and 11 existing warnings. New native tests cover per-contact ownership, SMS/MMS incoming watermarks, pending receipts, deletion fallback, late generation revisions and request identity.
- `tools/ui-manual-takeover.cjs` passes real touch sending while AI, saves and history are held pending; duplicate-tap coalescing; failed-send UUID reuse; follow-ups during delivery waits; preserved newer typing; out-of-order send results; late AI rejection; and attachment safeguards. Existing `ui-send-recovery.cjs` and `ui-inbox-touch.cjs` also pass.
- `tools/ui-send.cjs` passes exact-body UUID sends without draft-save waits, touch/mouse hold and cancellation, keyboard sending, separate timer approval, sending during pending jobs and idempotent explicit retries. The browser demo uses the same manual-send behavior and passes a fictional request replay/recipient smoke check.
- `tools/check-manual-takeover.py` exercises the actual database v16→17 additions, preservation of existing rows, selective cancellation of unsent AI/plan work, transaction rollback, durable per-contact incoming watermarks and concurrent request identity. Existing text-MMS draft, contact-guidance and outgoing-MMS storage checks pass.
- `tools/check-mms-downloads.py` passes the incoming download-store v1→2 migration, preservation of in-flight carrier work and persisted receipt identity after reopening. Receipt ordering is captured before queued provider work so a pre-takeover message cannot later release the takeover merely by finishing insertion.
- Signed APK verification passed for version 0.10.4/code63: unchanged signer and permissions, bundled assets/audio, protected components, disabled backup/debug/cleartext, 16 KiB alignment and known-credential exclusion. Evidence: `dist/installer-verification-0.10.4.json`. APK SHA-256: `a94a8f929796cbc18165cdb05e06a394ba3780beb3841392ad69d07c714a84d9`.
- Tests use fictional messages and mocked bridge/model responses. No real carrier message or live AI request is used. Physical Pixel sending and keyboard behavior remains to be checked on the device.

## 0.10.3 Live inbox activity — 2026-09-25

- `testReleaseUnitTest` and `lintRelease` passed: **594 unit tests**, zero failures/errors/skips; lint zero errors and 11 existing warnings.
- New native tests cover bounded provider scans even for one busy thread, mixed SMS/MMS timestamps, invalid/draft/control rows, independent inbox reads during blocked history/AI work, bounded refresh backlog, contact-access revision checks and serial receipt order while inbox work is blocked.
- `tools/ui-live-inbox.cjs` passes newest-first pinned/ordinary ordering, SMS/MMS sent/received activity during busy AI, partial new-thread merging, stale full/fast response races, authoritative deletions, burst coalescing, optimistic pin races, permission revocation and search/pressed-row preservation. Existing `ui-inbox-touch.cjs` and `ui-pins.cjs` also pass.
- `assembleRelease` and final installer verification are completed for version 0.10.3/code62. The same signer and permission set are retained; bundled assets/audio, private components, disabled backup/cleartext/debug, 16 KiB alignment and absence of known credentials are checked in `dist/installer-verification-0.10.3.json`. The installer ZIP holds the identical APK.
- No real messages or AI requests are used during these checks. Physical Pixel carrier timing still requires a device check.

## 0.10.2 Text MMS and reliable inbox touches — 2026-09-25

- `testReleaseUnitTest`, `lintRelease`, `assembleRelease` passed: **582 unit tests**, zero failures/errors/skips; lint zero errors and 11 warnings.
- Native classification tests distinguish complete text/plain and structural SMIL from real attachments, invalid or incomplete parts. Text-draft policy tests cover mixed SMS/MMS bursts, plans, location, bedtime, repeated acknowledgments, failed/outbox interleaving and exact source/recipient matching.
- `tools/ui-text-mms.cjs` passes text-only rendering, suppressed old media insights, verified text target including SMS base zero, same-base stale result rejection, expired generated draft clearing while preserving owner edits, foreground suggestions, exact-context no-reply retention, attachment/unknown-target guards and a visually reviewed 320px layout. Existing media and send-recovery browser regressions pass against the final UI.
- `tools/ui-inbox-touch.cjs` passes real mobile browser touch events during deferred inbox/full refreshes, cancelled gestures and long-press pin handling. Chat opens while native history responses are held pending; revoking access clears private data immediately. Existing pinning regressions pass.
- Signed installer checks are recorded in `dist/installer-verification-0.10.2.json`: version/code, unchanged signer and permissions, packaged assets, original Tiny Blast audio, protected components, disabled backup/cleartext/debug, 16 KiB alignment and known-credential exclusion. The installer ZIP contains the identical APK.
- Database v15→16 adds source bindings to generated text-MMS drafts. `tools/check-text-mms-storage.py` exercises actual migration SQL, unchanged existing drafts/location metadata, fresh-schema equivalence, reopen persistence and owner-edit clearing. Prior contact-guidance SQL checks still pass.
- No live contact, text-send or model request is used. The new MMS route prepares foreground drafts for review only, never a timer or automatic send. No service deployment is required.
- Physical Pixel carrier/provider and IME behavior remains untested. UI touch tests exercise real browser touch events against fictional native responses.

## 0.10.1 Sending, drafting and selection — 2026-09-24

- `testReleaseUnitTest`, `lintRelease`, `assembleRelease` passed: **557 unit tests**, zero failures/errors/skips; lint zero errors and 11 warnings.
- Native delivery policy tests cover complete multipart delivery settling a send, late callback precedence and unknown/failed recovery. `tools/check-send-outcomes.py` exercises the actual Store SQL for repair, reopening, receipt identity and approved-learning context anchoring. Eight touch-policy tests exercise fully rejected gestures, cancellation of an already delivered DOWN, and keeping the long-press release from immediately dismissing its own toolbar.
- `tools/ui-send-recovery.cjs` passes with explicit holds on snapshot/history/save/generate/status responses: stale Delivered jobs, editable next-message field while pending, immediate visible Drafting feedback, chat-local save barriers, failure preserving owner text, late suggestions, duplicate quick taps, fast native draft payloads, monotonic delivery, no backward base change and no restoration of deleted SMS from old jobs.
- Existing `ui-editor-regression.cjs`, `ui-pilot-training.cjs`, `ui-send.cjs` and `ui-reply-controls.cjs` pass. These retain practice/contact isolation, typed-node/selection survival, native context-menu behavior, send/hold/timer semantics and per-contact control checks. Removed Explain meaning controls are absent; retained legacy notes are not erased by forgetting practice.
- The generated APK and matching ZIP are verified separately for version 0.10.1/code60, unchanged signer, packaged asset equality, original Tiny Blast audio, private components, disabled backup/cleartext/debug, 16 KiB alignment and absence of known credentials. The only added permission is normal `android.permission.HIDE_OVERLAY_WINDOWS`; evidence is `dist/installer-verification-0.10.1.json`.
- No relay change or deployment is needed. AI requests still share one generation queue; UI feedback and returning the saved draft remove misleading waits, but server/network/model latency remains.
- Browser regressions use fictional bridge data only. No carrier message or paid model request is sent.
- Physical Android keyboard and floating Copy/Select all toolbar behavior cannot be exercised by a desktop browser; those changes need a Pixel check after installation.

## 0.10.0 Reply controls, editing and plans — 2026-09-24

- Final `testReleaseUnitTest`, `lintRelease`, `assembleRelease` passed: 542 unit tests, no failures/errors/skips; lint zero errors, 11 warnings. Local AI/ML Kit generation and dependencies are removed; OkHttp/Okio remain because link previews and location lookup use them.
- New ContactGuidance and expanded timer policies verify bounded fields, ask_me defaults, effective learned/girlfriend voice, explicit 1-second fixed timers and rejection of other sub-minute/custom/range values. Existing AttentionPolicy tests use the exact new canned Delay text.
- `tools/check-contact-guidance.py` passes against the actual Store SQL: v14→v15 preservation and fresh-schema equivalence, inactive legacy settings untouched, full details and one-second persistence, atomic concurrent once-per-hold claims, rollback/restart and rapid-message deduplication.
- Focused reply-controls and editor browser checks use fictional native bridges. They cover new fields and plans choice, girlfriend style fixed across one-second/range/immediate delays, removed tone/humor/local-AI UI, preserved editor identity/cursor, message tap vs native long-press selection, keyboard request priority and selection dismissal. No real AI or text sends are used.
- Existing readiness, setup/cache, sharing, Train Pilot, send/timer, attention-action and synthetic keyboard-viewport regressions pass. Updated legacy assertions reflect intentionally removed controls/new timer labels; existing history, identity, permission and no-send assertions remain. 320px screenshots are in `dist/reply-controls-320.png` and `dist/reply-controls-notes-320.png`.
- Relay suite: 126 mocked tests pass; six new contact-guidance checks pass after final wording. All draft/media/training paths accept bounded combined context and ignore retired style fields for `styleMode: learned`; legacy callers remain compatible. Docker runtime files/imports are allowlisted. Model, phone credential and API key stay unchanged. Deployment `69cabc22-15f6-49b0-ba81-e3746d389ca8` is successful. The authenticated health check accepts the existing phone token and reports contactGuidanceVersion 1 with gpt-6-sol; no live model request was made. Evidence is in `dist/contact-guidance-deployment-0.10.0.json`.
- Installer signer, permissions, private PlanDelayJob registration, bundled assets/audio, alignment and secret-pattern verification are recorded in `dist/installer-verification-0.10.0.json`.
- No physical Pixel IME/action-mode instrumentation or carrier SMS/MMS delivery test was performed. Browser keyboard sizing is simulated, and native keyboard/menu lifecycle was reviewed. Android background scheduling and the one-second alarm can be delayed by the OS; generation and incoming quiet time precede a send timer. The planning hold stays active after a canned deferral, and interrupted/uncertain automatic deferrals are not retried.

## 0.9.13 SMS/MMS readiness counting — 2026-09-24

- `testReleaseUnitTest`, `lintRelease`, `assembleRelease`: passed. 531 unit tests, zero failures/errors/skips. Lint: zero errors and 12 existing warnings. The latest native source was compiled into this release.
- Ten new ReplyReadinessPolicy regressions cover exactly 4 SMS (1 owner/3 incoming) plus older MMS reaching 20/5/5; owner replies beyond newest50; fair source scanning during SMS floods; automated SMS exclusion without dropping colliding MMS IDs; normalized/imported duplicates; invalid, unsent, placeholder and foreign-recipient rows; bounded incomplete scans; access revocation; imported examples; and unavailable MMS parts.
- Strict readiness uses verified current provider data, retained transport identity and recipient checks. It never trusts launch/archive caches. Text/row limits, early exit and per-speaker bounded counters avoid copying full archives. Group/unconfirmed display paths skip readiness; temporary provider readiness failures preserve loaded chats/settings while disabling eligibility. Strict checks remain in profile saves, automatic requests and dispatch.
- `tools/ui-reply-readiness.cjs`: passed. Fictional four-message case, initial checking state, independent recheck, fresh counts overriding old saved-sample analysis, unsaved canonical examples rechecked without replacing a typed log, incomplete/unavailable/error states, preserved notes and Off selection, late permission-loss response guard, 320px layout. No automatic settings write, AI or send. Screenshot: `dist/reply-readiness-0.9.13.png`.
- Existing `tools/ui-fast-setup-cache.cjs` and `tools/ui-share.cjs`: passed. Phone readiness/settings/history and Android-share review UI regressions use fictional bridge data.
- Release 0.9.13/code58 APK SHA-256: 8558679ca1ab30a2649c5c19f6c5d4aabee663aa12fa5a14784292bcee38cba3. Installer verification evidence is recorded in `dist/installer-verification-0.9.13.json` (same signer/permission set, compiled share registrations, matching assets including Settings version, disabled debug/backup/cleartext, private callbacks/provider roots, 16 KiB alignment, original Tiny Blast, known token/API-key pattern checks and ZIP payload).
- No physical Pixel history provider, actual contact content, real AI request or message send was used. Private RCS-only messages remain unavailable; supported provider text and labeled samples are the evidence source. Counters are lower bounds after early success or incomplete scans, not total conversation size. No service change required.

## 0.9.12 Android share menu — 2026-09-24

- Native `testReleaseUnitTest`, `lintRelease`, and `assembleRelease`: passed. 521 unit tests, zero failures/errors/skips; lint has zero errors and 12 existing warnings. Six new SharePolicy cases cover bounded/appended text, supported MIME categories, URI restrictions/contact-card export paths, stream deduplication and count limits.
- `tools/ui-share.cjs`: passed with fictional bridge data. Covers cold/warm shares, escaped text/link display, recent/manual/contact selection, explicit Add to draft, preserving owner text, image captions and attachment-only imports, recoverable errors across resume, cancellation during a stalled provider, duplicate/replaced share guards, lost-acknowledgment recovery that preserves newer edits, invalid formats, permission loss, and 320px layout. Asserts no SMS/MMS send or AI call. Screenshots: `dist/share-draft-0.9.12.png`, `dist/share-review-320-0.9.12.png`.
- Existing `tools/ui-chat-media.cjs` and `tools/ui-fast-setup-cache.cjs`: passed; normal media composition, suggestions, chat history and contact photos remain functional in the mocked browser environment.
- Native review: both share intent filters retain the required default-SMS components; external streams require explicit URI grants, private provider paths are refused, files are bounded and byte-validated, multi-file staging is transactional, and adding a share creates an owner-edited draft with automatic sending cancelled. Process recreation cannot recapture a consumed intent; idempotent retries return identity rather than stale draft contents. Native lifecycle/provider behavior was reviewed, not instrumented on a physical device.
- Signed release 0.9.12/code57 APK SHA-256: 6b2674937af99ec2da92f4c5b4fda2e1d4a97ce1e41d230d83868d5d89aff63d. Same signer and permissions as 0.9.11; compiled share intent filters, matching packaged assets, APK-in-ZIP bytes, disabled backups/debug/cleartext, private callbacks, file-provider roots, 16 KiB alignment, unchanged Tiny Blast and no known token/API-key patterns verified. Evidence: `dist/installer-verification-0.9.12.json`.
- Physical Pixel Sharesheet entry, actual third-party URI grants/cloud-backed files, Android activity recreation and carrier MMS delivery still require device testing. No real contacts, message content, AI calls or outgoing texts were used during validation. No hosted service change required.

## 0.9.11 History loading and archive recovery — 2026-09-24

- Reproduced and fixed the UI ordering failure: a live conversation could finish before the full inbox, then a delayed saved page replaced it with previewOnly data. Full inbox completion never repaired that state. Saved responses now require the conversation still be provisional. Live history pagination and gap filling can proceed while the inbox is pending, while send/AI gates keep their full readiness requirement.
- Failed saved pages retain their exact cursor. A changed committed archive snapshot is rebased once at the same keyset boundary, with existing recipient checks and progress validation. Other failures require explicit retry and cannot spin on scroll. A first archive rebuild returns a strictly content-free pending envelope instead of a misleading access error; the UI checks again on archive completion or explicit Check saved history.
- Unchanged full scans retain the public snapshot. Distinct scan markers preserve deletion reconciliation even across clock rollback. Message/header/recipient/deletion changes still advance snapshots. Existing encryption, transaction, schema and access rules remain.
- Live conversation/history/media-context reads have two workers and a four-item queue, separate from full inbox/carrier recovery. Foreground/default-role/READ_SMS and access epochs are checked before work and delivery. Already-read chats avoid zero-row provider writes that could repeatedly invalidate the archive; optional read-marker write failure no longer discards a successfully loaded chat. Permission failures still reject delivery.
- All 515 Java tests passed with zero failures/errors/skips (six new regressions). Release build and lint passed with zero lint errors and 12 existing warnings. SQLite archive checks passed over 700 conversations and a 2,410-message thread, including production SQL, WAL reads, rollback, persistence, deletion and recipient changes.
- Four browser suites passed: ui-history-recovery, ui-history-cache-gaps, ui-launch-cache, ui-fast-setup-cache. New test holds the full inbox while live/archived results return out of order; verifies live older pages, exact failed cursor retry, one snapshot rebase, typed pending rebuild, completion signal, delayed-read retry with preserved typing, and stale callback rejection. Existing history merging, deletion, identity, cache/media/send gating, editor/caret and bounded DOM tests passed. Fictional data only; no real SMS, contacts or AI request. The 412×915 recovery screen was visually reviewed.
- Actual Pixel/provider behavior remains unverified. These concrete code regressions match the reported state, but no phone stack trace was available. Initial full sync duration depends on phone history and provider speed. No new permission, hosted service change or re-pairing for an ordinary update; do not clear storage.

- Signed release 0.9.11/code56 APK SHA-256: 760e70a224b750ae0d09a5a7f9cf2239d2b88b5e3d1a6e5990288959b2efa50c. Same signer and permission set as 0.9.10; packaged assets, APK-in-ZIP bytes, disabled backup/debug/cleartext, private callbacks, FileProvider roots, 16 KiB alignment, unchanged Tiny Blast sound and absence of known pairing/API-key patterns verified. Evidence: dist/installer-verification-0.9.11.json.

## 0.9.10 Default-app setup recovery — 2026-09-24

- Fixed the UI holding “Waiting for Android” until a full message snapshot completed. Role results update setup immediately. Read-only smsRoleStatus runs independently on the UI lane and returns Android's actual role plus READ_SMS/SEND_SMS status. Resume and Check status reconcile even if the chooser callback is missing. Explicit Settings fallback is available during waits and retires old attempts.
- Native chooser attempts use distinct request codes, a 30-second deadline, return reconciliation and destroy cleanup. Timeout reports uncertainty, never a grant or fabricated denial. The app's explanatory dialog itself does not expire while the user reads it. Old callbacks cannot complete newer attempts.
- Request and status generations reject stale UI responses; fresh role/permission answers take precedence over older snapshots. Display access recovers only after actual role/read/send success, and access loss clears private displayed content. Existing native read/send/AI gates remain unchanged.
- All 509 Java tests pass (zero failures/errors/skips), including five new role lifecycle regressions. Release build and lint pass with zero lint errors and 12 existing warnings.
- ui-setup and ui-fast-setup-cache pass using fictional bridge data. Tests cover cleared-storage state, cancel/denial/error/timeout, manual Settings fallback, retry/old callback isolation, role gain/loss while history is blocked, stale status/snapshot protection, permission loss, fresh-access recovery, compact layout and no message/AI/settings mutations. Recovery controls were visually reviewed.
- No actual Pixel role chooser or carrier send was exercised. This fixes application waiting/recovery; it cannot override Android restrictions or restore app-private pairing/preferences/practice erased by Clear storage. No service change, new permission or database migration. Re-pair after clearing app storage, not after an ordinary update.

- Signed release 0.9.10/code55 APK SHA-256: df1e9538476624915813dd42a90eeda4cf78bd079ef1c1de7718feecc8cf21b6. Same signer and permissions as 0.9.9; packaged assets, installer ZIP bytes, disabled backup/debug/cleartext, private callbacks, FileProvider roots, 16 KiB alignment, original Tiny Blast sound and absence of known pairing/API-key patterns verified. Evidence: dist/installer-verification-0.9.10.json.

## 0.9.9 History gaps, available MMS text and training recovery — 2026-09-24

- Fixed real UI omissions: opening an archived latest-40 page no longer replaces longer previously loaded history; a provisional inbox snippet cannot hide a gap between saved rows and the newest live page. Cache/live gaps fetch immediately after hydration, paging starts at the last verified boundary and failed provider loads retry the same cursor even if a cached page said no more. Verified page ranges replace deleted cached rows. Recipient fingerprint changes still discard previous-person rows. Draft/caret, scroll anchors and the 240-node rendering window remain.
- Shared MMS text reads normalize MIME capitalization/parameters, support inline and file-backed text with charset handling and bounded bytes, preserve permission failures, and mark missing text unavailable. One unreadable file no longer aborts the entire archive; it does not promote uncertain old text as fresh. The flag and explicit text placeholder survive full archive, launch history and live views; real attachments remain visible. Cached/provisional media cannot open raw part URLs before live validation.
- Saved chat history in Settings shows actual conversation/message counts, last completed save, syncing/error state and an explicit refresh. Full archive updates remain atomic; provider/disk failures preserve the previous snapshot, back off at 15/30/60 seconds and then require another real invalidation or manual retry. Missing individual text files can coexist with a completed archive; affected rows say their text is unavailable. Counts clear on message-access loss and late status responses cannot restore them.
- Train Pilot now selects the newest 50 readable SMS/MMS texts together, with one older incoming fallback when needed. It has no age cutoff. File-backed/parameterized MMS text is eligible; unsent/failed/queued messages, notices, other recipients, empty images and unreadable partial text are excluded. Explicit Start and current recipient/message/settings/access/session checks remain. A failed start renders only one primary recovery button; secondary controls have more space.
- All 504 Java tests passed with zero failures/errors/skips; 22 new regressions cover mixed-context selection, older incoming, missing/partial text, access revocation, file/charset/size policy, retry backoff and launch flag propagation. Release build/lint passed with zero errors and 12 existing warnings. The existing real SQLite archive script passes its 700-chat/2,410-message persistence/paging/rollback checks.
- Seven browser suites passed: ui-history-cache-gaps, ui-history, ui-fast-setup-cache, ui-chat-media, ui-pilot-training, ui-cache-status and ui-keyboard. New tests reproduce archive61–100 plus newest251–300 with a misleading latest snippet, fill101–250, preserve all300 rows across reopen, retry a failed31 boundary without skipping, remove deleted cached row10, clear prior-recipient history, show unavailable MMS text, preserve attachments, show only one practice retry action, update saved counts and reject late access-revoked status. Tests use fictional data and no real contacts/messages/AI/SMS. Recovery screenshot was visually reviewed.
- This fixes accessible SMS/MMS history handling; it does not claim to import private history from another app or inaccessible RCS. Actual Pixel/provider behavior and full-inbox sync duration remain unverified on device. No new permission, pairing, hosted service/model change or database migration.
- Signed release0.9.9/code54 APK SHA-256: 4a733a9a86074aeed46afabe79fe6a0e6e99e7de84912dfe2f97bb9afe532421. Evidence: dist/installer-verification-0.9.9.json. Update in place, then inspect Settings → Saved chat history and optionally Refresh saved history.

## 0.9.8 Contact photos beside saved conversations — 2026-09-24

- Fixed the missing launch-cache photo annotation: LaunchInboxCache.load now attaches current local contact-thumbnail routes after normalizing saved summaries and applying pins. The encrypted cache still stores no photo URLs/bytes; foreground/Contacts/SMS/default-role and existing delivery checks remain. Live/archive inboxes already provided routes, explaining why a photo could appear after opening a chat while its launch row showed initials.
- The first twelve rendered inbox rows request eager thumbnails, including pinned chats; later rows remain lazy. Avatar metadata preserves this loading policy when Contacts changes invalidate images. Existing local route validation, bounded decoding/caching, missing-photo initials and permission revocation are unchanged.
- All 482 Java tests passed with zero failures/errors/skips. Release build and lint passed with zero lint errors and 12 existing warnings.
- Three browser suites passed: ui-inbox-photos, ui-fast-setup-cache and ui-launch-cache. The new test holds live/archive results and confirms twenty launch rows, pinned/unpinned photo loading before any chat read, eager/lazy policy, invalidation, missing-photo initials, search/rerender, narrow layout and Contacts revocation with no further image loads. Existing archive/settings/current-provider/access guards pass. Initial attempts found the local preview stopped; it was restarted and both checks passed.
- Fictional illustrated thumbnail screenshots at 412×915 and 320×470 were reviewed. No real contact/message/photo, AI request or carrier send was used. Actual Android Contacts Provider behavior and physical Pixel display still need phone testing. No hosted service/model/pairing change is required.
- Signed release 0.9.8/code 53; APK SHA-256: 8b86b5355ccb80ab088b4beddb8333e23c94f973200fdd0cbbaba7dacc8331d8. Installer evidence: dist/installer-verification-0.9.8.json. Install as an update over the existing app.

## 0.9.7 Composer training and independent practice loading — 2026-09-24

- Train Pilot moved from the contact header to the reply composer, immediately above the text input. Its panel opens while live conversation/history readers are pending; Start practice remains an explicit action after current native contact validation. Read-only state does not invalidate the pending conversation read. A lesson saved during that read retains its updated count without discarding the live chat or typed draft.
- Practice state and scenario generation have dedicated bounded workers, independent of the inbox/media/shared AI queues. State reads immutable session snapshots outside the send lock. Generation rejects additional queued requests rather than accumulating network calls. Busy is published before provider/context gathering and cleared on preparation/network errors. Native expected-recipient, session/turn tokens, message freshness, settings revisions, foreground and permission checks remain, including at actual UI delivery.
- Closing and reopening during first/next-turn generation shows current progress and polls only local native state, every 1.5 seconds for at most 40 polls; further progress checks are explicit. A failed first turn can start a new scenario; a saved reply retries its existing token. Pause, access changes and contact navigation close/discard stale UI work. No automatic cloud call is introduced by opening/polling.
- Signed release build passed; all 482 Java tests passed with zero failures/errors/skips. Four new executor tests exercise independence, no queued duplicate generation, bounded state queues and route isolation. Lint passed with zero errors and 12 existing warnings.
- Five browser suites passed: ui-pilot-training, ui-keyboard, ui-typing, ui-fast-setup-cache and ui-refinement. New training checks hold live chat/state responses, save a lesson before hydration, reopen during initial/next-turn generation, recover a failed first turn, bind every request to its expected contact and discard late pause/navigation responses. Existing typing/IME/caret, real-draft isolation, meaning tokens, exact approval, cache/settings guards and responsive layout checks pass. The former cached-history assertion that training was disabled was updated to its new independent, explicit-start behavior. Composer screenshots at 412×915 and 320×470 were visually reviewed.
- Checks use fictional bridge data and local unit tests; no actual contact text, AI request or carrier send was used. This removes confirmed queue and hydration waits in code, but actual Pixel/network latency is not benchmarked. Online scenario generation still takes time. Existing relay/model/pairing are unchanged.
- APK SHA-256: f05557205c166edac2a6cd872333bf63ee7407c8492a19ee5e3afb14662ce150. Installer evidence: dist/installer-verification-0.9.7.json. Version 0.9.7/code 52 updates over the existing installation.

## 0.9.6 Attachment-only Media tab — 2026-09-24

- Fixed the screenshot's text-only media cards: `Messages.multimedia` now requires a valid file part before appending a gallery row, counting it toward the 60-card window or looking up its contact. Only real sent/incoming/queued/failed message envelopes are eligible; drafts, pending notices and protocol reports stay out of Media. Ordinary MMS text remains untouched in the inbox/history.
- Native and UI classification normalize MIME capitalization and parameters, retain image/audio/video/vCard/PDF/other file attachments, and exclude plain text, HTML, SMIL presentations, multipart containers, invalid/wildcard MIME types and invalid part IDs. Plain-text captions remain beside attachments. The UI filters both incoming gallery state and rendered cards, including cached legacy rows; empty results now say No attachments yet. Download notices remain in the existing chat flow until actual attachments are available. This changes no send behavior, provider history, cache, pairing or hosted service.
- All 478 Java tests passed, zero failures/errors/skips; five new gallery regression tests cover text/control exclusion, real file types, captions, case/parameters, invalid metadata/IDs and envelope eligibility. Lint passed with zero errors and 12 existing warnings.
- Three browser suites passed against the actual 18769 assets: ui-media, ui-media-context and ui-chat-media. Gallery tests include 160 text-only rows before real media, image/audio/video/PDF/vCard/file preservation, pending/report/draft exclusion, captions, legacy cached state filtering, text-only MMS remaining in chat, no false empty state during loading, lazy images, errors/retry, navigation and editor preservation. The fixture-only 412×915 gallery screenshot was visually reviewed. No real message, attachment, contact, SMS or AI request was used; physical Pixel/carrier testing remains outstanding.
- Signed release installer verified as 0.9.6/code 51, same signer as 0.9.5, unchanged permissions, exact packaged assets, preserved Tiny Blast, private components/FileProvider paths, disabled backup/debugging/cleartext, 16KiB alignment and no known pairing/API-key pattern. APK SHA-256: 60b276590c3456604a2b84fb38b1d38724213c7450c16f3ff9f9b0fa84a634a3. Evidence: dist/installer-verification-0.9.6.json. Update in place; no new permission or re-pairing is required.

## 0.9.5 Independent Reply setup, contact photos and full history archive — 2026-09-24

- Reply setup opens immediately for the active chat. Its own bounded native worker resolves the recipient and reads current settings independently of conversation hydration. Edits survive late reads; saves check current canonical recipient and profile revision under the existing send lock. A group chat keeps the control available but explains its read-only limitation. Settings access never makes cached history a send base or AI authorization.
- Android contact thumbnails appear in the inbox, chat header and contact picker. Strict local routes resolve a phone number through Contacts Provider, never arbitrary URIs or external picture hosts. Thumbnail dimensions/bytes, concurrent decoding and RAM are bounded. Photos invalidate on contact/access/lifecycle changes, with initials fallback and checks at stream consumption. Batch inbox annotation avoids per-row permission/role lookups.
- Added a separate no-backup SQLite archive for all available SMS/MMS chats and full message text, with encrypted per-record content and bounded paged reads. Media descriptors are retained; image/audio/video bytes remain in the provider. A low-priority background worker scans and reconciles provider data in a transaction; WAL readers retain the old completed snapshot during refresh. Recipient identity staging rejects mid-scan thread reuse. Permission loss blocks/deletes previews; contact loss scrubs names; authentication/missing-key failures rebuild unreadable cached ciphertext. Ordinary provider failures preserve the last committed archive.
- Removed the live inbox's 150-row limit. The UI initially renders 120 inbox rows, with more on demand, and at most 240 timeline messages at a time with older/newer navigation. Cached reads stay display-only, bind recipient/snapshot identity, preserve typed replies and yield to live results. Cached media IDs cannot open raw media or authorize explanation/actions before live verification. Photos and settings do not wait for full chat history.
- All 473 Java tests passed with zero failures/errors/skips. Lint passed with zero errors and 12 existing warnings. The 22 new policy/cipher tests cover thumbnail routes/access/bounds/invalidation, full-history cursors/budgets/access and real AES-GCM nonce, AAD relocation, wrong-key, tampering, Unicode and size behavior.
- `tools/check-history-archive.py` executes the actual production schema and paged SQL with fictional data: 700 chats, 2,410 mixed messages in one chat, long text beyond the old limit, repeated date ties, WAL readers, rollback, deletion/orphan reconciliation, recipient changes, empty-page snapshot identity, reopen persistence and ciphertext clearing before key/authentication recovery. It does not exercise Android's actual Keystore or carrier provider.
- Thirteen browser suites passed: ui-fast-setup-cache, ui-refinement, ui-launch-histories, ui-launch-theme, ui-contacts, ui-pins, ui-pilot-training, ui-typing, ui-keyboard, ui-chat-media, ui-send, ui-inbox-live and ui-history. New cases cover 360 summaries / 380 cached messages, independent settings while both live readers are held, preserved edits, repeated revision-checked saves, permission/photo fallback, cached media placeholders without part reads, stripped cached meaning tokens and recipient-fingerprint changes. Existing history assertions now verify full 273/274-message records and access through a 240-bubble window, including overlapping anchors and intact draft/caret. Stale approved-learning counts found in regression were corrected. 412×915 and 320×470 screenshots were visually reviewed.
- Signed release installer verified as version 0.9.5/code 50, same signer as 0.9.4, unchanged permissions, matching packaged assets and original Tiny Blast sound. Private components/FileProvider paths, disabled backups/debugging/cleartext, 16KiB alignment and absence of known pairing/API-key patterns passed. APK SHA-256: 8923f59d950c0a578e7be9125f55f23b850dd9064be6fd51b7b56e873d50c93b. Evidence: dist/installer-verification-0.9.5.json.
- Tests used fictional data. No real contact/photo/message, location, AI request or carrier send was used. No hosted deployment or pairing change is required. Physical Pixel cold-start latency, contact-photo availability, first-sync duration and storage usage still need device testing.

## 0.9.4 Train Pilot and message insights — 2026-09-24

- Added Train Pilot beside the contact name. An explicit Start practice shares up to 50 recent SMS texts plus scoped guidance with the existing relay; received contact turns guide the simulated speaker. Practice is labeled fictional and never becomes SMS history, a send base, an approval, or a scheduled message. Up to eight owner answers per 30-minute session; 24 bounded examples per contact persist privately. A retry uses the native turn token and cannot save the same lesson twice. Replies are saved before requesting another practice message.
- Received SMS/retrieved MMS can be held to explain meaning. Native-generated identity tokens bind the visible transport, provider ID, thread, date, text and media parts; saved notes are revalidated against the actual provider message before reuse. Up to 40 notes per contact, 600 characters each. SMS/MMS ID collisions remain separate. Meaning notes and practice are additionally scoped by normalized contact-address hash to prevent thread reuse from mixing people. Editing/removing a meaning requires the same current message identity. Forget practice preserves separate message notes and real history.
- New lessons/insights invalidate in-flight generation and stop pending automatic timers for that person; manually edited drafts are retained. Planning holds, loop protection, readiness and opt-in rules are unchanged. Practice never changes those settings. Guidance is included as untrusted, historical context for later cloud drafts/media analysis, and bounded examples also guide optional Nano. Actual current messages override fictional scenarios; notes cannot supply current availability, personal facts or sending authority.
- Removed About me settings UI, native save bridge/snapshot and prompt use. Myself (beta) emphasizes actual sent and owner-written practice examples. The relay accepts bounded legacy personality input for compatibility but does not send it to the provider. This is contextual personalization, not model fine-tuning.
- Signed release build, 451 Java tests and lint passed: zero test failures/errors/skips, zero lint errors and 12 pre-existing warnings. Fifteen new policy tests cover session tokens, retries, bounds/Unicode/sanitization, contact isolation, eligibility, expiration and retired-profile exclusion. Nine SQLite checks pass, including actual historical migrations through v14 and new persistence/retention/uniqueness/edit/forget behavior with fictional data. Existing jobs, drafts, profiles, receipts and holds survive migration.
- Nine browser suites passed: ui-pilot-training, ui-personal-context, ui-refinement, ui-typing, ui-keyboard, ui-chat-media, ui-send, ui-launch-theme and ui-launch-histories. Training checks include lost-response retry recovery, no send calls, contact navigation races, exact meaning tokens, SMS/MMS ID collision, selection/scroll behavior, removal, inert background/focus, and 412×915 / 320×470 layouts. Screenshots were visually reviewed; actual Pixel touch and keyboard behavior still need device testing.
- All 120 relay tests pass using mocked provider responses and localhost HTTP. /train shares authentication, rate/day/concurrency limits and request identity checks, uses structured output, has no tools and sets store:false. Successful retries reuse output; failed practice requests may retry immediately. Sanitized guidance cannot promote itself into instructions or remove existing draft safeguards.
- Deployed nine allowlisted service files to the existing Railway service, deployment 23f4a3fa-bbb0-44a3-be2e-f8a97d50792e, status SUCCESS, one replica, sleeping disabled. Keys/pairing/model were preserved. Live authenticated health advertises pilotTrainingVersion:1 and messageMeaningsVersion:1. Fictional gaming text passed initial practice, identical retry, next turn and guided draft; an unauthenticated request returned 401. No actual contacts, messages, location or SMS were used. Evidence: dist/training-deployment-0.9.4.json and dist/training-live-0.9.4.json.
- Installer checks passed: version 0.9.4/code 49, unchanged signer and permissions, exact packaged assets, original icon/sound, private components, no backup/cleartext/debugging, 16KiB alignment and no known phone credential/API-key pattern. APK SHA-256: 77d348d60bea3ff4bde190decfa07ca8f443826e84c94672dd4b0496d31e78b2. Evidence: dist/installer-verification-0.9.4.json. No physical phone installation or real carrier send was performed.

## 0.9.3 Supplied launcher artwork — 2026-09-24

- Integrated the user-supplied 1254×1254 PNG unchanged in source, with an 8.564815% adaptive foreground inset (9.25dp within 108dp). Both normal/round launcher references retain the adaptive wrapper. A matching native vector R silhouette replaces the previous themed-icon glyph. Notification sound and separate status glyph are unchanged.
- Signed release build and lint passed: zero errors and 12 existing warnings. Installer checks confirm version 0.9.3/code 48, the existing signer, unchanged permissions, packaged UI assets, original Tiny Blast, private components and release settings. APK SHA-256: b28d64cd8da4ba7283d0fc1ffcef26acb38ddc4e675cb7480d1fd235794742c6.
- Decoded packaged PNG pixels exactly match the supplied image (AAPT changes PNG encoding only). Circle, rounded-square, square and themed-icon previews were visually inspected. The measured color mark radius is 32.862dp within the 33dp safe radius. Evidence: dist/icon-verification-0.9.3.json, dist/installer-verification-0.9.3.json and dist/launcher-icon-0.9.3.png.
- This update changes launcher resources and version labels only; prior messaging tests were not rerun. No physical Pixel install or launcher-cache refresh was exercised, and no real message, location or AI request was made.

## 0.9.2 Theme gesture stability, accurate snippets and saved histories — 2026-09-24

- Theme selection keeps a stable button/check DOM through snapshots and defers Settings replacement during the gesture. Theme palettes are scoped to the app root and swatches. Selection updates immediately; one write is active and only the latest pending choice is retained. Stale write failures/snapshots cannot roll back a newer choice.
- Actual bounded MMS text/plain content supplies live snippets; attachment-only/pending MMS keeps an honest fallback. Ambiguous grouped provider rows use actual per-transport latest rows, with consistent SMS milliseconds/MMS seconds and draft/protocol-report exclusions. Launch summary format2 preserves text and deliberately rebuilds old generic-only snapshots once.
- A separate reader loads encrypted saved history without querying messages. A coalesced background worker selects the10 latest threads by message date, reads bounded latest eligible SMS/MMS rows, merges the30 latest entries per thread, and persists only allowlisted text/metadata. Bodies are limited to2048UTF-16 units, pages to10×30 and plaintext to2MiB. Attachment bytes, parts, drafts, profiles, jobs, send bases, SIM choices and AI permission are excluded. Long texts are marked shortened; live history still supplies complete available content.
- AES-256-GCM/Android Keystore with a separate alias/AAD, AtomicFile and private no-backup storage protect the cache. No new FileProvider root or permission is added. Access, sequence and provider-change guards invalidate stale writes/delivery; contacts-denied startup scrubs stored names. Only successful whole refreshes replace history. Failed/provider-busy reads keep the last good cache; authoritative empty lists remove it. First use must seed the file. Background warming never marks read, drafts, sends or calls AI.
- UI launch cache reads run alongside the live snapshot; either inbox/history order is supported. Saved pages remain display-only and cannot replace a live conversation or resurrect an absent/revoked thread. Hydration preserves typed text/caret. A synthetic initial ready notification before the first authoritative snapshot does not discard a slow disk preview; subsequent provider changes retain stale-result guards.
- Final signed Android build and **436 Java tests passed**, zero failures/errors/skips. Lint reports zero errors and12 existing warnings. The29 added policy tests cover MMS text/fallback, eligible transports, date/order boundaries, cache snippet preservation, most-recent10 selection, newest30 chronology/deduplication, cross-thread/type filtering, Unicode/body bounds and exclusion of drafts/protocol reports. Cache application-context retention is explicitly bounded to the worker; no Activity is retained.
- All13 focused/affected browser suites passed: ui-launch-theme, ui-launch-histories, ui-launch-cache, ui-instant-chat, ui-typing, ui-keyboard, ui-history, ui-link-previews, ui-pins, ui-refinement, ui-send, ui-chat-media and ui-inbox-live. Final focused reruns additionally pass actual nested-control touch taps, latest-choice write coalescing, snapshot-between-pointerdown/up and rapid second-gesture target preservation; history tests cover parallel/both-order reads,10×30/reload bounds, pending startup versus later provider events, live/empty/access/name precedence, no cached authority and preserved editable composer/caret.
- Regenerated theme/history screenshots at412×915 and320×470 were visually inspected. Fictional message data only; no remote website, actual message, geocoder or AI call was exercised. Synthetic first-frame results are not Pixel latency claims.
- Installer verification passed: version0.9.2/code47, unchanged signer/permissions, matching packaged assets, private callback components and FileProvider roots (no history cache), non-debuggable release, backups/cleartext disabled,16KiB alignment, original Tiny Blast intact and no known phone token/API-key pattern. Evidence: dist/installer-verification-0.9.2.json. APK SHA-256ccd5ebc74831d978fdc267aed7093fb04bcd8d22f58daa8d178a90eaa3ac1389. No cloud deployment or re-pairing required.
- Physical Pixel cold-launch timing, Android Keystore/disk persistence, actual SMS/MMS provider behavior and touch latency still need device testing. Browser timing is synthetic; no real message, location or AI request was made for this update.

## 0.9.1 Cleaner chat rows — 2026-09-24

- Removed chat-row three-dot controls and their reserved space. Long press, right-click and Shift+F10 still open the pin dialog; focus restoration targets the chat row or previous editor/search. Native storage, messaging, location and AI behavior are unchanged.
- Signed Android build and lint passed (zero errors,12 existing warnings). Updated existing ui-pins suite passed, covering no overflow buttons, long press/right-click/Shift+F10, normal opening, immediate pin changes, saved state, stale results, failure recovery and preserved focus/scroll. Screenshots at412×915 and320×470 were inspected with no overflow. No new unit tests were added for this UI-only change; the prior407 native tests are unchanged.
- Installer verification passed with unchanged signer/permissions, matching assets, private callbacks, disabled debugging/backup/cleartext, unchanged Tiny Blast and no packaged known token/API-key pattern. Evidence: dist/installer-verification-0.9.1.json. No physical-device test or real messages were used.

## 0.9.0 Immediate themes, responsive pins and confirmed home — 2026-09-24

- Saved Android theme is applied to the launch HTML and native window before UI initialization. The optional browser demo loads its non-private theme from local storage before CSS. Theme changes have a dedicated durable worker and stale-snapshot protection; other settings no longer write a possibly outdated theme.
- Pin/unpin updates immediately without a full inbox read. Per-thread ordered saves and mutation barriers preserve the newest intent through delayed acknowledgments and stale snapshots. Failures restore the last acknowledged choice and offer Retry. Native pin validation avoids holding the preference lock during provider reads, and encrypted launch summaries receive current persistent pin IDs before delivery.
- Use current place now opens a confirmation dialog, obtains a fresh precise fix, and offers up to ten actual nearby numbered street-address records ordered by distance. Search alone never saves home. Explicit confirmation fills the field from the acknowledged address; manual entry retains unit/apartment details. Tokens expire and are invalidated by cancellation, pause or relevant settings changes; GPS age/accuracy, revision, permission and foreground checks repeat at commit. Field edits remain independent of delayed results.
- Final signed Android build and **407 Java tests passed**, with zero failures, errors or skipped tests. Lint reports zero errors and12 existing warnings. Nineteen new tests cover theme-before-CSS injection/identifier validation and home candidate precision/mock checks, actual numbered streets, distance ordering/ten-result limits/deduplication, token binding, revision/cancellation/expiry/two clocks/reboot and immutable candidate choices. Five new regular expressions compiled successfully with ICU78.3. An initial API33-only stream helper was replaced with a bounded compatible read for Android12.
- Eight browser suites passed: ui-launch-theme, ui-home-address, ui-pins, ui-personal-context, ui-launch-cache, ui-instant-chat, ui-typing and ui-keyboard. Held native callbacks exercise first-render theme selection, rapid preferences, optimistic pin ordering, serialized writes, stale snapshots, disk failure rollback/Retry, real scrolled anchors and preserved search/editor identity/caret. Home checks cover a ten-choice limit, no save before Yes, exact request/candidate binding, acknowledged field contents, cancellation/failure/manual fallback and late-result isolation. Browser checks use a fictional native bridge; no actual SMS, GPS or AI was used.
- Launch-theme and home-address screenshots at412×915 and320×470, plus pin-list/menu screenshots, were visually inspected. Small-height dialogs scroll and have no horizontal overflow. These checks do not measure native Pixel launch time or frame rate.
- Packaged APK/versionCode45, matching packaged assets, APK-in-ZIP bytes, unchanged signer and permission set, non-debuggable release, disabled backups/cleartext, private callback components, FileProvider roots and16 KiB alignment were verified. Tiny Blast remains byte-identical. Known phone token and API-key patterns are absent. No deployment or re-pairing is required. Evidence: dist/installer-verification-0.9.0.json; APK SHA-2561538f6c2a2ea0da52a986af317d55f35768453531deb65205def3f170f120132.
- Physical Pixel cold launch, disk persistence across process restarts, GPS/geocoding coverage and address accuracy still need device testing. No real GPS, address-provider, SMS/MMS or AI request was made for this update. Existing carrier checks remain outstanding.

## 0.8.9 Tappable links and public preview cards — 2026-09-24

- Fresh SMS/MMS text and historical media captions now linkify explicit HTTP/HTTPS URLs. Each message can display one card for its first link, with public page title, website name, short description and thumbnail. The actual destination host remains visible and clicking always opens the original URL through Android. Multiple URLs remain separately tappable. Provisional inbox snippets stay plain until fresh conversation data arrives, preventing cut-off URLs from becoming clickable destinations.
- Visible cards load on independent background workers after live message details are ready. An unavailable/private/login-only page, unsupported metadata or insecure HTTP preview leaves a compact domain/arrow fallback. HTTP links remain explicitly openable. Cards do not execute page scripts, play embedded video, authenticate to social accounts, change drafts or invoke AI. Settings → Link previews defaults on, discloses direct website requests and can disable cards/fetching while keeping link tapping.
- The bridge validates thread/transport/message/URL against the actual provider body before and after network work. Foreground, permission, default-app role, settings and activity/native generations protect queued requests, active fetches, image delivery and final WebView responses. Only public HTTPS port443 can be fetched; literal IPs and every DNS answer, redirect and image destination are checked. Redirects are manual and bounded to three. Credentials, executable schemes, ambiguous URLs, local/private/reserved addresses and insecure redirects are rejected. No cookies, proxy, authorization, referrer, JavaScript or HTTP2 connection coalescing is used.
- Native limits: two preview workers with six queued tasks, two bounded DNS workers with two-second waits, a 12-second network budget, 256 KiB HTML prefix, 1 MiB complete image input, 16-million-pixel source bound, 768-pixel output edge and 150 KiB re-encoded JPEG. Large HTML bodies can supply their beginning without full download. Failed/slow images retain valid text metadata. Memory caches hold at most40 URLs and3 MiB image bytes, with ten-minute success/one-minute failure expiration. Concurrent identical URL commits reuse a stable successful image token. There is no disk cache or cloud-service deployment for this feature.
- The UI requests at most two visible cards concurrently and retains at most80 bounded metadata entries. Expired entries are retried on a later scheduled visible visit, without periodic network polling. Late results cannot update another chat or survive access loss/opt-out. Pausing invalidates unfinished requests; resume can retry them. Card-only updates preserve the editor, cursor, focus and timeline anchor; local image failures retain a usable text card. Request-only media-history references are released after completion.
- The final signed Android build and **388 Java tests passed**, with zero failures, errors or skipped tests. Twenty new policy tests cover URL/canonical/message extraction, schemes/credentials/control characters, IPv4/IPv6/private/mapped/translation ranges, mixed DNS answers, redirect/image validation, metadata/entity/fallback parsing, malicious base URLs, login walls and size/text bounds. Lint reports zero errors and12 warnings. An initial lint failure for a newer CharSequence method was fixed with a compatible length check. Enabling required parser desugaring exposed older compiler metadata warnings; the compatible AGP8.13.2 patch eliminated them. Final compiler metadata warnings:0. References: [jsoup Android support](https://jsoup.org/download), [Android compiler compatibility](https://developer.android.com/build/releases/agp-8-13-0-release-notes).
- Nine browser suites passed: ui-link-previews, ui-launch-cache, ui-instant-chat, ui-send, ui-typing, ui-history, ui-chat-media, ui-attachments and ui-keyboard. Focused checks cover correct original-link opening without waiting for draft saves, metadata escaping, result URL binding, trusted local lazy images, social/HTTP fallback, multiple links, rejected schemes, MMS binding, stable typing/reading position, stale/access-loss isolation, visible-only fetching, max-two concurrency, bounded cache after94 distinct URLs, opt-out, and launch freshness. The final rerun verifies the compact fallback shows its domain only once. Browser remote requests were blocked and none occurred; no actual SMS/MMS or AI call was made.
- dist/link-previews-412x915.png and dist/link-previews-320x470.png were regenerated and visually inspected. The white thumbnail is an intentional fictional image fixture, not a fetched social post. Cards stay within the conversation, with ordinary scrolling in the reduced keyboard-height viewport and no composer/horizontal overflow.
- Installer verified as **0.8.9/code44**, same signer as0.8.8, unchanged permissions, private callbacks, disabled backup/cleartext, non-debuggable,16 KiB alignment, packaged assets matching source and unchanged Tiny Blast audio. Known secret checks passed. APK SHA-256: `d2b25b4f74a03aa3304a4a61e903a267bbebd092ca9cc137f40080098763ce7c`. The install ZIP contains that exact APK; report: dist/installer-verification-0.8.9.json.
- Actual Pixel intent routing, website availability, native DNS/TLS behavior, Android image decoding and social-site responses still need device testing. The automated network checks here exercise validation rules and fictional browser/native fixtures, not real Facebook/Instagram content. Install over the existing app, try an ordinary public HTTPS page and one social post, tap each destination, then check an unavailable link and the Settings opt-out. No new pairing or API key is required.


## 0.8.8 Saved recent conversations on launch — 2026-09-24

- After a successful inbox read, the app saves display summaries for the 20 most recent threads by message date, independently of pin ordering. A dedicated bootstrap read starts alongside the live snapshot so closing/reopening the app can show this saved list before provider reads finish. The first launch after this update needs one successful inbox load to seed the new file. Before any saved/live data exists, the UI uses a neutral placeholder rather than claiming the inbox is empty or setup is missing.
- Storage is a bounded AES-256-GCM encrypted AtomicFile in the private no-backup directory, with a separate Android Keystore key. Summaries retain only allowlisted IDs, addresses, contact names, times, read/pin flags and short snippets. MMS stores a generic media preview. Full histories, attachments, profiles, drafts, jobs, cloud settings, permissions and SIM state are excluded. Asynchronous writes coalesce; unchanged rows avoid repeat disk writes. A successful empty inbox clears the file. Temporary provider failure, corrupt data or key/storage errors do not masquerade as an empty live inbox.
- Access/revision checks protect read, write and final WebView delivery. SMS/default-app access loss blocks old results and queues deletion. Contact revocation removes names immediately and scrubs disk data; restored permission can save current names again. A deferred scrub cannot supersede a newer pending authoritative save. Cached rows are display-only, never sending authority. A cached unread row says Unread rather than implying an AI draft is ready.
- The first successful live snapshot always wins, including an empty/deleted inbox. Late cache callbacks cannot restore removed rows. Clicking a saved row preserves typing while both the current global snapshot and live conversation load; send/AI/setup/attachment actions remain gated. A removed cached-origin thread cancels its pending conversation result. Initial refresh failures keep the saved list and offer Retry. Existing automatic-message updates continue to refresh live rows and the next saved snapshot.
- Release build and all **368 Java tests passed**, with zero failures, errors or skipped tests. Eleven new policy tests cover selection by recency before pins, ordering/deduplication/reentry, invalid IDs, Unicode-safe clipping, bounds, contact-name removal, media redaction and empty input. Lint reports zero errors and 12 warnings; the added StaticFieldLeak warning concerns the short-lived pending writer record, which retains only getApplicationContext(), not an Activity. Its field is cleared when consumed by the writer.
- Nine browser suites passed: ui-launch-cache, ui-instant-chat, ui-chat-load, ui-send, ui-typing, ui-keyboard, ui-history, ui-chat-media and ui-attachments. The focused launch fixture holds live reads to verify 20 visible saved rows, simulated reload persistence, independent requests, stale/empty/deletion precedence, permission loss, safe draft ownership, retry, corrupt/absent cache fallback, allowlisted fields, and no AI/sending/prefetch side effects from startup cache. Final rerun covers Unread during bootstrap and unchanged authoritative badges. All data is fictional; no real SMS/MMS or AI request was made.
- Screenshots dist/launch-cache-412x915.png and dist/launch-cache-320x470.png were regenerated and visually checked with no clipping or horizontal overflow. The final synthetic sample painted on the first frame in 9.6 ms after the fixture delivered cached rows. This measures neither Android disk/Keystore latency nor Pixel launch performance. Real encrypted persistence across process restarts, device permissions/lifecycle and provider/storage contention still require phone validation.
- Installer verified as **0.8.8/code 43**, same signer as 0.8.7, unchanged permissions, private MMS callbacks, backups/cleartext disabled, non-debuggable, 16 KiB alignment, source-matching assets and unchanged Tiny Blast. The cache is outside the FileProvider roots. Known phone-token/API-key checks passed. APK SHA-256: `d4289f51b76b1eb44a8a3b4798bd4de1e05acc4e091bad678465cf853cdd9a86`. The install ZIP contains that exact APK. Report: dist/installer-verification-0.8.8.json.
- Install over the existing app, open once until the conversation list appears, then close/reopen and verify the latest 20 previews appear while the live list refreshes. Message changes while the app is closed may briefly lag in this saved display. No new API key, pairing, cloud deployment or message database migration is needed.


## 0.8.7 Immediate chat display and background preloading — 2026-09-24

- Opening a conversation now renders its available history immediately, before draft storage or the live provider read finishes. A first cold visit uses the actual latest inbox message; it does not invent earlier history. Recent/pinned/visible conversations warm in small background batches. The opening spinner is removed; an inline update/retry notice keeps the chat and editable text field visible. Recent cached scroll position and previously loaded history are reused.
- The UI retains at most 24 inactive conversation previews within an estimated 4 MiB serialized-data budget. Native previews retain at most 12 pages / 2 MiB with a 30-second expiration and a 512 KiB per-page limit. Native warm-up has a dedicated low-priority bounded worker, while the UI requests at most three threads at once. Partial/missing previews do not block other threads or cause retry loops. Older pages remain on-demand, and media bytes are not preloaded.
- Previews never mark messages read, use AI, modify drafts or authorize sending. The live conversation still reads a fresh provider base/profile/holds before enabling actions. Native caches and pending responses invalidate on provider/contact changes, message events, permissions and foreground transitions, with a final access/revision check before WebView delivery. UI epochs reject late warm/live/snapshot results and clear cached private content on message/default-app access loss. Contact revocation removes old names and invalidates pending reads.
- Navigation no longer awaits draft acknowledgments. Captured saves remain bound to their original thread/base; typing during a preview is buffered until the actual base arrives. Hydration preserves the editor, cursor and owner text. Acknowledged empty drafts do not permanently suppress fresh suggestions. Chat browsing and typing remain available when a different chat has a busy operation. Generation retains its original conversation through any pending save. Confirmed MMS captions are not resurrected from a stale local edit when the native draft is already cleared.
- Large bridge responses are serialized on the worker before UI posting. Live conversation setup now reads one newest SMS row instead of loading/annotating an additional full SMS history just to find its base. There is no persistent message database migration, extra permission, cloud service deployment or credential change.
- Android release build and all **357 Java tests passed**, including ten new cache-policy tests for expiration, clock rollback, permission/revision invalidation, late loads, LRU/byte limits, replacement accounting, oversized rejection and clearing. Lint reports zero errors and 11 warnings.
- Six existing browser regressions passed: send gestures, typing, keyboard, full history, chat media and attachments. Their original live-operation checks now explicitly wait for hydration; asynchronous navigation saves are checked after acknowledgment. The history fixture retains 274 loaded messages through switches and covers authoritative empty reads and late pages. Tests use fictional bridge data and make no actual SMS, carrier or AI requests.
- Focused immediate-open/retry tests cover cold/warm display with the native read deliberately held, no opening spinner, writable provisional text, correct-base save, cursor identity, pending-save navigation, cross-chat/same-chat late responses, memory bounds, permission clearing and failed refresh recovery. A synthetic desktop sample painted at the first animation frame in 13.1 ms; this is not a Pixel latency measurement. The 412×915 pending/ready views and 320×470 / 412×915 error views were visually inspected with no clipping or overflow.
- Installer verified as **0.8.7/code 42**, same signer as 0.8.6, unchanged permissions, private MMS callbacks, backups and cleartext disabled, non-debuggable, packaged assets matching source and unchanged Tiny Blast audio. Alignment and known-secret checks passed. APK SHA-256: `7407b40955ae7fcc039cb6c8e4fdd36d7dfe3b89c541bd8e3e864abf17514099`. The install ZIP contains this exact APK. Report: `dist/installer-verification-0.8.7.json`.
- The final focused rerun also passed switching/typing while a different chat has an active generation, delayed Redraft followed by navigation without retargeting the request, and clearing a confirmed same-base MMS caption without losing newly typed preview text. Physical Pixel frame times, very large archives, actual provider/storage contention and background/resume behavior still need phone validation. Install as an update and try quickly switching several chats while typing.


## 0.8.6 Carrier attachment sending and receiving — 2026-09-24

- The paperclip opens Android's document picker for one selected file at a time. An individual conversation can stage up to six supported photos, GIFs, short videos, audio files or contact cards with an optional caption. Photos are decoded with orientation handling, flattened and re-encoded within the selected SIM's carrier limits. Sending is explicit and immediate; attachment scheduling, RCS and group sending are not added. Staged outgoing files are not uploaded to AI.
- Outgoing MMS has a separate private SQLite database, bounded file copies, persistent request IDs, atomic attachment claims and exact provider/transaction binding. The private carrier callback requires a successful matching SendConf response to mark Sent; transport success alone is insufficient. Missing/ambiguous evidence remains unknown without automatic retry. Terminal cleanup and interrupted preparation recover across restarts. An unknown identical payload stays duplicate-blocked after discarding and re-adding it.
- Incoming notices persist before carrier download, deduplicate repeated WAP pushes and track the actual receiving SIM. Downloads have bounded parsing, per-message progress/errors and explicit retry. A validated private PDU and digest allow a partial provider write to be repaired locally on retry. Download timeout/restart recovery does not start network work. Carrier transaction suffix settings and protocol acknowledgments are handled through Android's MMS service. These acknowledgments are not recipient read receipts.
- Native history, media and inbox paths include MMS status. The UI keeps the caption and selected files during pending/failing sends, prevents stale picker results from moving to another conversation, supports empty captions and explicit failed-send retry, and preserves request identity across an uncertain bridge response. Received passive file types can open through a bounded private copy and read-only Android viewer grant. Selection invalidates in-flight AI and pauses automatic replies; confirmed MMS prevents a suggestion for the already-handled SMS base. A WAP receipt is marked before background parsing so automatic and MMS send gates can wait for incoming context.
- Five browser suites passed: `ui-attachments`, `ui-send`, `ui-typing`, `ui-keyboard` and `ui-chat-media`. They use fictional bridge data, with no carrier or AI calls. The focused suite covers picker/recipient races, send errors and uncertainty, limits, escaped text and narrow layouts. Screenshots `dist/attachments-412x915.png` and `dist/attachments-320x470.png` were visually inspected; the caption, send control and history remain visible without horizontal overflow.
- Outgoing and incoming SQLite checks passed using the actual new schemas: claim rollback, duplicate request protection, provider identity, callback replay, stale tokens, persisted outcomes, local repair, one-shot protocol acknowledgment and interrupted notice binding. Existing `pilot.db` remains schema13; no existing profile, pairing or SMS data migration is required. Four bounded viewer-copy tests plus outgoing/incoming policy cases are included in the Android unit suite.
- Release build and all **347 Java tests passed**, with zero failures, errors or skipped tests. Lint has zero errors and 11 warnings. The added tests cover 16 incoming policies, 12 outgoing policies and four bounded viewer-copy cases. No hosted service deployment, key change or real SMS/MMS send occurred.
- Installer verified as **0.8.6/code41**, with the same signing certificate as0.8.5, unchanged permissions, non-debuggable, backups/cleartext disabled, private MMS callback receivers and private FileProvider paths. Packaged assets match source, Tiny Blast is unchanged,16KB alignment passes, and known phone-token/API-key patterns are absent. Report: `dist/installer-verification-0.8.6.json`. APK SHA-256: `dafb0984dcaea20383e5bab498800077da5bf83e72210ff28e6e995f08a7fca3`. The install ZIP contains that exact APK.
- **Physical-device validation is still required.** No test here proves actual carrier interoperability, Android provider persistence, picker lifecycle or device codec behavior. Install the update in place, make Reply Pilot the default texting app, select the SIM and enable mobile data. With a willing helper, test one small photo each direction, then an optional caption and another supported file. Verify failed/disabled-data states and incoming Retry before relying on attachments. Sent means carrier acceptance, not recipient delivery or reading.


## 0.8.5 Softer color themes — 2026-09-24

- All ten palettes now use less saturated dark surfaces, muted accents and off-white foreground text. Header/composer surfaces remain separate from the chat history; Midnight remains the default and existing theme selections are preserved. The saved toast uses a subdued surface instead of an inverted bright fill, warning orange is softened, and disclosure focus outlines use the chosen palette. Native window/status/navigation/startup colors match the web backgrounds; the native default accent matches Midnight.
- One-off palette measurements cover primary, secondary and accent text on background, paper, soft, incoming-bubble and selected surfaces, plus outgoing text and accent foreground pairs. Every checked normal-text pair is at least 4.5:1 (minimum 4.53:1). All ten native background constants match CSS. Report: `dist/themes-soft-contrast-0.8.5.json`. This is a scoped text-contrast check rather than a full accessibility audit.
- A bounded browser check selected all ten themes and measured 180 rendered text/background pairs (minimum4.53:1), confirmed theme persistence after reload and found no page errors or horizontal overflow. Seven screenshots were visually inspected: Midnight inbox/chat/settings/Reply setup, Rose and Forest chats, and a320×470 Midnight chat with the composer/send control fully visible. Report: `dist/themes-soft-report.json`; screenshots: `dist/themes-soft-*.png`. The preview used fictional data only.
- Release assembly and its critical lint checks passed. This is a color/copy update; no new unit tests were written and the broader messaging/AI suites were not rerun. The hosted service, permissions, database, reply behavior and connection remain unchanged.
- Installer verified as0.8.5/code40, same signer as0.8.4, unchanged permissions, non-debuggable release settings, private protected action components, backups/cleartext off,16KB alignment, matching packaged assets and unchanged Tiny Blast audio. Known phone-token/API-key patterns are absent. Install ZIP contains the exact APK. SHA-256: `15b70268bc7b2e5ac7502fbc95d6f443f140095621f5c26bacec5f9dcfe38e94`. Report: `dist/installer-verification-0.8.5.json`. Physical Pixel appearance has not been checked; perceived comfort varies with screen brightness and viewing conditions.


## 0.8.4 Media in chats, media understanding and approved reply examples — 2026-09-24

- Ordinary conversations now use a mixed SMS/MMS timeline with stable transport/id keys, normalized dates and bounded backward paging. The inbox includes media-only conversations and pinned media threads. Exact media navigation remains available. SMS send bases and delivery receipts stay independent from MMS IDs; ambiguous/group recipients remain read-only. Media renders inline; local audio/video controls use no autoplay and no eager preload. The private part endpoint validates MIME and numeric IDs, checks SMS permission, streams bounded ranges and closes descriptors; it does not copy attachments to public storage.
- Incoming-media understanding is an explicit per-contact cloud feature, also available for the latest incoming media when in-app suggestions are enabled and the editor is empty. Images/screenshots are resized and re-encoded to bounded metadata-free JPEGs; short supported videos provide up to three sampled frames. Unsupported, oversized, partial or unreadable attachments are disclosed. Audio is not transcribed or analyzed. Capture verifies the exact incoming row and recipient, uses an isolated bounded decoder and expires stale snapshots. Profile, consent, connection, revision, foreground state and new-message checks discard obsolete results. Opening or analyzing historical media cannot pause a current conversation.
- The hosted media route returns a description, explicitly tentative intent, limitations and a short review-only suggestion. Visible text, profile notes and examples are untrusted data. No media result is queued or sent automatically. Planning results for the current incoming media create a persistent hold; bedtime, current-location and unknown-personal-fact safeguards remain. Incomplete caption/unanswered context withholds a confident reply. New incoming MMS stops stale SMS generation and dispatch, including final cloud/Nano completion checks. UI results bind the mixed latest-message key as well as SMS base, and never silently replace typed text.
- In-app SMS suggestions default on only for an already enabled, connected contact; the setting does not grant cloud consent or enable Autopilot. Suggestions are attempted once per fresh incoming SMS, after quiet-burst and current-state checks. Existing drafts, no-reply decisions, holds and pending work prevent repeated generation. Manual typing, incoming-message refresh and cross-contact navigation retain owner drafts and discard stale AI output. Latest-media conversations show the media interpretation action instead of offering a draft based only on earlier SMS.
- With Match my style enabled, up to twelve recent distinct final manually approved and successfully sent replies for that contact become bounded request examples. Pending, failed, unreviewed automatic and canned Delay messages do not qualify. The app reuses existing job records; it does not fine-tune a model or add a database migration. Forget examples records a durable per-contact cutoff, invalidates in-flight automatic generation and preserves the chat itself. Future successful manual sends can add new examples. Examples are writing context, not evidence of current facts or authorization to send.
- Final release build and **315 Java tests passed**, with zero failures, errors or skips. Android lint reports zero errors and eleven existing warnings. Coverage includes mixed transport paging/order, media input bounds, strict analysis parsing, review/commitment gates, successfully sent example eligibility and forgetting, Unicode limits and local range handling. All **eight SQL migration checks passed** through unchanged database v13. All **109 relay tests passed**, including authenticated media input validation, strict output shapes, JPEG/JSON bounds, no arbitrary fetches, shared budgets, request identity, quoted inputs, approved examples and unchanged safety gates.
- The new browser media suite and five existing suites (history, typing, receipts, send and media-context) passed. Fixtures cover mixed IDs, media-only/ambiguous threads, image display and playback-control attributes, suggestion races, new media while a request is pending, cross-contact navigation, retained typed drafts, current-message binding, Forget controls and 320/412px layouts. Screenshots `dist/live-media-412x915.png` and `dist/live-media-320x470.png` were visually inspected. Browser fixtures do not establish native decoder, provider or codec compatibility.
- Railway deployment `9acc4069-4337-42d4-8ea1-206b1fa613b2` succeeded with seven allowlisted runtime/build files and unchanged credentials, endpoint, model and single replica. Authenticated health reports `mediaAnalysisVersion:1` and `approvedLearningVersion:1`; unauthenticated health rejects. All **seven fictional live checks passed**: image description/inside-joke reply, image-only invitation held for input, sampled-frame/no-audio disclosure, withheld unknown location, approved writing examples, SMS planning hold and bedtime stop. Synthetic graphics were used, not personal photos. Reports: `dist/media-live-0.8.4.json`, `dist/media-deployment-0.8.4.json`. No SMS was sent. These cases establish integration, not guaranteed interpretation or personality fidelity.
- Installer verified as **0.8.4/code39**, with the same signer as0.8.3, unchanged permissions, non-debuggable release settings, private protected action components, backups/cleartext off,16KB alignment, matching packaged assets and unchanged Tiny Blast audio. Known phone-token/API-key patterns are absent, and the install ZIP contains the exact APK. SHA-256: `c940a596da81cd41ddefb94479d5bd7ff5741453a231f704f66a6abd35c51150`. Report: `dist/installer-verification-0.8.4.json`. This update has not been installed on the user's Pixel during this task. Physical provider history, WebView playback, keyboard, MMS reception and carrier behavior still require device verification. RCS access and outgoing MMS are not added by this release.


## 0.8.3 Per-contact humor and inside jokes — 2026-09-24

- Reply setup adds a five-step Humor slider (Light · PG-13, Playful, Sarcastic, Edgy, Extreme) and a 2,000-character Inside jokes field. Changes remain staged until Save profile; existing tone, relationship, cloud consent, reply mode, timing and engagement stay unchanged. The UI updates slider labels and note counts in place, preserving focus and scroll. Notes are HTML-escaped. Every contact has independent saved values, with Light/empty defaults for older profiles.
- Native bridge/storage and relay strictly validate numeric integers 0–4 and text within 2,000 UTF-16 characters. Database v13 adds only the two relationship fields. Legacy save overloads preserve existing humor preferences; profile revision changes retain existing automatic-job and action invalidation. MainActivity captures omitted preferences under SEND_LOCK. Regular cloud drafts, automatic drafts and explicit Joke use the same captured contact profile. Optional Nano drafts include quoted notes and fixed humor guidance; generic local quick suggestions remain unchanged.
- Fixed prompts treat intensity as a ceiling rather than a joke quota, subordinate to serious context and stated boundaries. Inside jokes are untrusted per-contact callback context, not instructions, permanent training or proof of current facts. Extreme permits welcome adult innuendo and stronger language but excludes graphic sexual content and sexual jokes about minors. Existing planning, bedtime, location, readiness, length and no-reply guards remain. The service cache identity includes both new fields.
- Final release build and **290 Java tests passed** with zero failures/errors/skips. Lint: zero errors and eleven existing warnings. Nine new validator tests plus two prompt tests cover strict types, text limits, legacy defaults, quoted instruction-like notes and contact isolation. The existing bounded-history test now accounts for fixed humor guidance and proves that 1,000 large turns produce exactly the same prompt as the bounded latest turns; notes remain independently capped. All **eight SQL migration checks passed**, including actual v12→v13 preservation/fresh-schema equivalence, reopened state, revisions, contact isolation, attention actions, holds, drafts and job provenance.
- All **94 relay tests passed**, including strict inputs, old-client defaults, per-contact cache identity, quoted notes, all five humor levels, personality precedence and preserved planning/bedtime/location/output gates. Browser humor, attention, girlfriend and planning-hold fixtures passed. Humor checks cover 320/412px layouts, keyboard interaction, typing/slider focus, save failure, refresh races, contact isolation, defaults and demo persistence. `dist/person-humor-320.png` and `dist/person-humor-412.png` were visually checked.
- Railway deployment `6119ef76-8516-4799-b721-1bd647ed7d37` succeeded with seven allowlisted runtime/build files, unchanged endpoint/model/credentials and one replica. Authenticated health reports `contactHumorVersion:1`; unauthenticated health rejects. All **six fictional live checks passed**: a Light callback, Extreme callback, serious-message response, planning hold, bedtime stop and withheld unknown location. Both callback replies used the supplied five-star-chef joke; the serious reply stayed sincere. This small sample establishes integration, not guaranteed humor quality or recognition. Reports: `dist/humor-live-0.8.3.json`, `dist/humor-deployment-0.8.3.json`. No real SMS, contacts or location were used and no texts were sent.
- Installer verified as 0.8.3/code38, same signer as 0.8.2, unchanged permissions, non-debuggable release, private action components, backups/cleartext off, 16KB alignment, matching packaged assets and unchanged Tiny Blast audio. Known phone-token/API-key patterns are absent; ZIP contains the exact APK. APK SHA-256: `d5564ba196c0ffd20acba69b250d904688fdf8c072acce8e687568485665f0e8`. Report: `dist/installer-verification-0.8.3.json`. The update has not been installed on the user's Pixel during this task; actual WebView/keyboard behavior and local Nano output still need device validation.


## 0.8.2 Attention actions — 2026-09-24

- Attention notifications and in-chat hold banners offer Joke and, for plans, Delay. Joke adds a strict one-request owner interpretation to the current unanswered sequence and saves a draft for review only, even with Autopilot enabled. Incoming keyword shortcuts can be reconsidered, while output commitment/length checks, privacy, bedtime, unknown-fact and actual-planning handoffs remain. A Joke draft does not clear a persistent planning hold. Delay explicitly approves only “I'll let you know in a bit.”; it uses the ordinary carrier path, needs no AI connection, and never selects a plan or time.
- Store v12 adds attention_actions and jobs.attention_kind. Opaque action tokens bind thread/base, exact incoming fingerprint, saved profile, burst token and (for Joke) AI connection. Atomic claims reject replay. Delay job creation, deferral marker and token linkage commit together before dispatch; recipient, message, hold, active SIM and pending incoming receipts are rechecked before the carrier call. Linked/unknown Delay attempts cannot reoffer. Deferrals preserve the planning hold and five-reply cap and are excluded from owner-style evidence. Fixed text carries no location provenance and leaves existing drafts untouched.
- Actions use explicit immutable PendingIntents to a non-exported receiver and require authentication. The receiver also rejects locked-device execution. Notification privacy preferences remain in effect; Android controls lock-screen action rendering. [Android action authentication API](https://developer.android.com/reference/android/app/Notification.Action.Builder#setAuthenticationRequired(boolean)). Joke runs through a private persisted JobService instead of keeping a broadcast alive for a model call. Delay runs immediately on the IO worker and does not require network access.
- The native review found and fixed two lifecycle gaps: startup/resume/recovery now drains queued Joke work after a commit-before-schedule crash, without retrying claimed actions or any Delay; completed silent decisions and genuine current-request failures replace the progress notice. Interrupted AI results fail their final token check. A failed, unlinked action can receive a new opaque token for another explicit click; the old token cannot be reused. Profile/message changes and manual typing invalidate obsolete work or preserve the current draft.
- Final Android release build and **279 Java tests passed**; lint reports zero errors and eleven existing warnings. Twelve focused attention-policy tests passed. All seven migration checks passed, including historical upgrades through v12, exact fresh-schema comparison, data preservation, two-connection claim race, atomic-link rollback, stale-token rotation, deferral hold/cap exclusions and reopened queued/recovery state. These exercise policy and actual SQL, not the Android scheduler or a real carrier.
- All **84 relay tests passed**, including strict optional hint validation, unchanged ordinary requests, hint-aware cache identity, stale cached location rejection and retained safety checks. Railway deployment `ab080e63-4fc5-4cd2-b4fb-96ab3b2fd988` succeeded with seven allowlisted runtime/build files, unchanged endpoint/model/credentials and one replica. Authenticated health reports attentionActionsVersion:1; unauthenticated health rejects. Six fictional live cases passed: ordinary planning hold, absurd-plan joke, assistant-phrase joke, real planning despite Joke, unknown personal fact, and bedtime stop. Reports: `dist/attention-live-0.8.2.json`, `dist/attention-deployment-0.8.2.json`. No SMS was sent by these checks.
- Browser attention, planning-hold and girlfriend suites passed. Fixtures cover exact Delay copy, token-only calls, duplicate taps, errors, no automatic generation/send, manual-text/caret preservation, current vs stale responses and 320px/keyboard layouts. `dist/attention-actions-320x640.png` and `dist/attention-actions-412x470.png` were visually checked. Native notification appearance is not simulated by these browser fixtures.
- Installer verified as0.8.2/code37 with the same signer as0.8.1, unchanged permissions, private attention receiver and protected JobService, non-debuggable release settings, backups/cleartext off,16KB alignment, matching assets and unchanged Tiny Blast. Known phone-token/API-key patterns are absent; ZIP contains the exact APK. APK SHA-256: `ec0b58616ba9b2ab4f03d0d2746ec2157e6f862e6cd97e1c002c25f9eb986906`. Report: `dist/installer-verification-0.8.2.json`. The update has not been installed on the user's Pixel during this task. Physical lock-screen authentication/rendering, process lifecycle, carrier delivery and background timing still require device validation. Already carrier-submitted SMS cannot be recalled. No real contacts, texts or location were used during development.


## 0.8.1 Girlfriend Autopilot — 2026-09-24

- Added a saved per-contact preset that selects Partner, cloud drafting and zero-delay automatic sending with girlfriend engagement. The existing history threshold remains required. Its prompt requests brief warm grounded replies and at most one relevant follow-up; serious relationship questions wait for owner input. It does not fabricate first-person stories, feelings or promises.
- Phone and relay share bedtime/stop recognition and conservative resume rules. Direct goodnight, going to bed/sleep and requests to stop end replies with no final message; acknowledgment/emoji follow-ups retain the pause, while a newer substantive incoming message can resume. Quoted/historical/negated examples do not act as direct bedtime instructions. Model instructions cover more contextual variants, but language recognition cannot be guaranteed for every phrasing.
- Private per-thread preferences store only pause state and processed SMS ID. The state survives restarts; explicit Resume preserves that watermark and saves a no-reply decision for the old message. SMS insertion, pause changes, profile saves and carrier-boundary checks share SEND_LOCK. Existing pre-IO receipt marking invalidates an in-flight reply or old timer before its final send check. Persistence failures fail closed; manual messaging remains available. Already carrier-submitted SMS cannot be recalled.
- Final release build and **267 Java tests passed**; lint reports zero errors and eleven warnings (ten prior warnings plus an intentional off-main-thread synchronous pause preference commit). Nineteen focused engagement/pause tests and all 17 new regexes were also checked independently with ICU 78.3. Six existing SQL migration checks passed through unchanged Store v11. No new permissions, schema migration or credentials were added.
- All **75 relay tests passed**, including HTTP authentication, request-id caching, legacy modes, history/planning/location checks, output limits, bedtime plus plans, reaction-only follow-ups and substantive resumption. The existing strict Responses decision format and model settings remain.
- Browser fixtures for girlfriend preset/pause, personal context and planning hold passed. These verify explicit Save, per-contact fields, mode changes, failed saves/resumes, no generation while paused, stale-contact responses, preserved manual edits and narrow/keyboard layouts. Final screenshots were visually checked at 320px and 412px. Independent read-only review found no actionable native integration issue.
- Railway deployment `001f064d-afd4-4d1b-a71e-658198758475` succeeded. Only seven allowlisted runtime/build files were uploaded; endpoint, model, credentials and one-replica configuration remain unchanged. Authenticated health reports `girlfriendModeVersion:1`; unauthenticated health rejects. Eight fictional live cases passed: direct bedtime, bedtime plus reaction, bedtime plus plans, planning handoff, fresh substantive conversation, unknown personal facts, serious relationship question and negated bedtime. Reports: `dist/girlfriend-live-0.8.1.json` and `dist/girlfriend-deployment-0.8.1.json`.
- Installer verified as0.8.1/code36, same signer as0.8.0, unchanged permissions, non-debuggable, private protected services/receivers, backups/cleartext off, matching assets,16KB native-library alignment and unchanged Tiny Blast sound. No known phone-token or API-key patterns were found. APK SHA-256: `672acb1fae103d1a41d73f397f995e300ae81787e60964c3c855118e6db6e436`. ZIP contains that exact APK; verification record: `dist/installer-verification-0.8.1.json`.
- No real SMS, contacts, location or personal conversations were used. Physical Pixel installation, carrier timing, Android preference disk failures and actual background receipt behavior remain on-device checks.


## 0.8.0 Media context, personality, engagement and optional location — 2026-09-24

- Media navigation resolves the actual MMS ID back to its current provider thread/date. It opens the exact anchor among 20 earlier/20 later mixed SMS/MMS rows, then pages 40 at a time. SMS milliseconds and MMS seconds, equal timestamps, independent IDs, bounded parts/text, cursor membership, deletion/permission changes and read-only group/ambiguous destinations are handled. This history view does not alter the live send base, composer, draft, profile or timer; Latest conversation is explicit and does not request automatic generation.
- Natural, Always reply and Keep conversation going are per-person preferences. Higher engagement permits one ordinary acknowledgment, then suppresses further acknowledgment-only texts until substantive incoming input; unanswered acknowledgment bursts consume no allowance before a real sent reply. Existing planning holds, history eligibility, unsupported-task/oversized-output guards, duplicate suppression and five-reply cap remain. Imported samples and About me are bounded quoted context, not instructions or permanent training. Myself (beta) emphasizes owner voice/humor; humor quality remains contextual and cannot be guaranteed.
- Location is off initially and each contact requires separate explicit sharing permission. Foreground precise/coarse permission is requested separately; background access is an Android settings choice. A private JobService schedules roughly 20-minute refreshes, subject to Android delays. Location acquisition, geocoding and bounded HTTPS map requests run off the SMS/AI workers. The app stores only its latest fix plus optional home, never a movement history; coordinate/home fields are absent from its WebView snapshot and AI payload. Android geocoding and OpenStreetMap receive the data needed for their disclosed lookup functions.
- Location validity uses wall/elapsed time, boot identity, permissions, service state, accuracy and a 25-minute maximum age. Home requires a precise fix within a conservative distance/accuracy radius; Use current place requires a fix no older than two minutes. Typed home lookup requires one street/house-number result. Mock/invalid fixes cannot be shared. Label-only context is attached solely to current-location questions for an allowed contact. Location-derived draft and queued-job revision/expiry are captured together; queueing, activation, refresh/recovery and the final carrier boundary recheck them. Both automatic and untouched manually approved AI location timers cannot outlive that evidence. Revision-changing location commits share SEND_LOCK with carrier submission, while network/geocoder work remains outside it.
- Android release build and all **255 Java tests passed**. Lint: zero errors, ten warnings (the extra warning is the intentional off-main-thread synchronous location preference commit). The first lint run exposed an unrecognized indirect permission guard; the native location call now explicitly handles SecurityException. Independent review found and fixed the location-revocation/carrier-check race before the final passing build. Existing and new native regexes were checked with ICU 78.3, including location detector patterns.
- Six browser suites passed: media context, personal context, typing, send gestures, planning hold and pins. Fixtures cover exact media anchor, paging, stale navigation, failed settings saves, saved vs newer unsaved edits, focus/cursor preservation, master-off location controls, no implicit consent or background API request, narrow layouts and prior send/hold behavior. About me, location settings and media anchor screenshots were visually checked. These use fictional bridge data and do not measure physical Pixel keyboard/GPS behavior.
- Six SQL migration checks passed through Store v11, including historical v1–v10 paths, natural/off/zero defaults, unchanged drafts/jobs/receipts/holds, 14-argument profile upsert isolation and location revision/expiry/due persistence across later draft/profile changes and database reopen. A fresh install and upgraded schema agree.
- All **68 relay tests passed**, including mocked model calls and loopback HTTP security. The initial live location fixture was held; a narrowly anchored simple-location path now formats only the approved coarse label directly after the existing guards. Mixed requests still require contextual model review and exact-label validation. No private prompt/output debugging or content logging was introduced.
- Final Railway deployment `0796be58-f778-4ac0-80c7-268cb7907a8b` succeeded; only six allowlisted runtime/build files were uploaded. The endpoint, model (`gpt-6-sol`), credentials and one-replica configuration were retained. Authenticated health reports personalizationVersion:1/locationVersion:1 and unauthenticated health rejects. Seven fictional live cases passed: first acknowledgment, repeated-ack silence, keep-going follow-up, personal humor, location without consent, approved coarse location and planning handoff. Reports: `dist/personal-context-live-0.8.0.json`, `dist/personal-context-deployment-0.8.0.json`. Initial failed-location evidence is retained separately; no actual location was supplied.
- Installer verified as 0.8.0/code35, same signer as0.7.24, non-debuggable, backups/cleartext off, private location JobService protected by BIND_JOB_SERVICE, and exactly three new optional location permissions. Packaged assets match source, native libraries pass16KB alignment, Tiny Blast is unchanged, and known phone-token/API-key patterns are absent. APK SHA-256: `76ce442f397ceb9d1969c15143dfe60c3cb0968b2daebab707c5fe75169d32e7`. The ZIP contains that exact APK. Report: `dist/installer-verification-0.8.0.json`.
- No real location, phone contacts, SMS, geocoder or map query was used during development. The app has not been installed on the user's Pixel during this task. Android location grant/revocation, real MMS/provider behavior, background scheduling, geocoder/landmark availability and carrier delivery require physical-device validation. Location recognition is a recent approximate fix, not continuous live tracking.

## 0.7.24 Pinned conversations — 2026-09-24

- Added a per-row three-dot menu, long press, right-click/keyboard context menu, Pin/Unpin action sheet and visible pin marker. Menu actions wait for a successful native save before reporting success. Movement, scrolling and early Android context-menu events suppress the long-press release click. Overflow targets are at least 44 pixels wide; Escape/Back/Cancel and keyboard focus are handled.
- Pins are private SharedPreferences thread IDs, committed by the IO worker. Invalid threads, missing SMS access and deleted conversations are rejected when pinning. Unpin can remove a stored ID even after history disappears. A failed commit attempts to restore the previous in-memory set and reports failure. No database migration, new permission, SMS/contacts write, AI opt-in or service change was introduced.
- The native inbox includes all existing pinned chats plus up to 150 recent unpinned chats. Each group sorts by newest message date and ID. Old pins are filled through bound exact-thread latest-row queries; duplicate and deleted threads do not produce extra/empty rows. Receipt annotation is batched to avoid SQL variable limits when many chats are pinned. Search continues to filter both groups. Browser-demo pins persist separately in localStorage.
- Release build and all **217 Java tests** passed, including nine new pin-policy cases for ordering, duplicate rows, old pins beyond the recent window, more than 150 pins, invalid IDs, overflow-safe comparisons, and unpin restoration. Lint: zero errors and nine existing warnings. Synchronous preference commits are intentional on the IO worker so failed saves can be reported.
- Six browser suites passed: focused pins, live inbox, contacts, typing, send gestures and media/search. Pin fixtures cover successful and failed saves, reload persistence, stale snapshots, early context menus, pointer movement/scroll cancellation, search caret, keyboard accessibility, escaped names, narrow layouts, demo persistence and preservation of draft/profile/timer/recipient state. Three existing test selectors were narrowed to `.row` to distinguish the new sibling options button. Initial selector/setup failures were fixture issues and passed after correction.
- Visually checked `dist/pins-inbox-preview.png` and `dist/pins-menu-320x470.png`; `dist/pins-menu-preview.png` is also available. Fixtures use fictional contacts only. No real messages, contact records, SMS sends, live AI requests or service deployment were used.
- Installer verified: 0.7.24/code 34, same signer as 0.7.23, unchanged permissions, non-debuggable, private delivery/Sleep receivers, backups/cleartext disabled, 16 KB alignment, matching packaged assets, unchanged Tiny Blast audio and no known phone-token/API-key patterns. APK SHA-256: `bf7e5ef7874292a9a6487cc4e49fe416ab458a75f9154ede3b31d2d025c1820e`. Verification record: `dist/installer-verification-0.7.24.json`.
- The update has not been installed on the user's Pixel during this task. Actual Android contact/SMS-provider behavior, preference disk failures and physical-device long-press gestures remain on-device checks; browser persistence and Java policy fixtures do not replace those checks.

## 0.7.23 Planning handoff and incoming-message bursts — 2026-09-24

- Native and relay planning checks return an empty `plans_need_input` decision. Common invitations, availability, confirmations, changed plans and AI commitments are held before generation or before accepting its output. Model instructions require contextual handoff for subtler planning language. Explicit AI drafts follow this rule too; manually written sends/timers remain available.
- Store v10 adds contact-level holds with the first planning-message ID and a monotonically extended latest-incoming watermark. New texts, edits, profile saves and restarts preserve the hold. Only approved manual carrier acceptance or aggregate delivery evidence through the current watermark clears it. A successful manual retry after a failed outgoing row can clear it; older callbacks, automatic sends, partial/failed/unknown outcomes and pending newer receipts cannot.
- Incoming SMS receipt metadata is marked before queued IO/SEND_LOCK, then bound to the inserted message. A persisted local 10-second quiet interval restarts for each arrival. Per-contact job IDs, receipt tokens and exact-row addresses prevent stale jobs/results/errors from replacing newer work. New input stops an old automatic timer; generation and carrier gates recheck freshness, including after outgoing provider insertion. Already carrier-submitted SMS cannot be recalled, and a final OS/carrier boundary race cannot be eliminated.
- Cloud context retains all consecutive unanswered texts (up to 50, each up to 1,600 characters), including with style matching off. Oversized bursts require review instead of omitting earlier messages or corrections. No extra historical conversations are selected.
- Release build and all **208 Java tests** passed. Lint: zero errors and nine existing warnings. New cases cover planning classification/commitments, strict empty decisions, complete bursts, overflow, receipt ordering/clocks, stale generations and manual retry coverage. ICU 78.3 compiled all 26 planning literals; existing Android whitespace/request-guard checks passed. These are compiled/pure-policy and ICU tests, not a device runtime test.
- All **48 relay tests** passed, including fictional loopback HTTP with mocked model calls. All **six SQL checks** passed: existing delivery/profile/loop/Sleep/range migration checks plus actual v9→v10 SQL and planning-hold persistence/release fixtures. Data, drafts, original-message pointers and unrelated contacts stay isolated.
- Browser checks passed for planning hold, reply stopping, keyboard layout, typing, chat-load recovery, random range and Sleep. Fictional bridge checks cover no automatic send/generation during a hold, provider-only clearing, failed manual sends, retained original text outside the current page, HTML escaping, quiet-period results, no UI retries, focus preservation and narrow/keyboard viewports. Hold screenshots were visually checked. They do not measure Pixel keyboard latency.
- Railway deployment `1bc87d69-64da-4be5-8c46-2cbe63c30b68` succeeded. Only five runtime/build files were uploaded, including the new planning module. Existing model, endpoint, pairing token and private variables were unchanged; one replica remains configured. Authenticated health reports `planSafetyVersion:1`; unauthenticated health is denied.
- Five fictional live checks passed: direct invitation, split invitation followed by thanks, changed time, contextual invitation, and a four-message correction where the reply used the corrected name. The last two used the model. Records: `dist/planning-live-0.7.23.json` and `dist/planning-deployment-0.7.23.json`. No real SMS or personal conversations were used.
- Installer verified: 0.7.23/code 33, same signer as 0.7.22, unchanged permissions, non-debuggable, private delivery/Sleep receivers, backups/cleartext disabled, 16 KB alignment, matching assets, unchanged Tiny Blast audio, known phone-token/API-key patterns absent. APK SHA-256: `b553ab477b75bb9cf3538e0cb3a932f7f03f876d7ca2ddd15d8297f38ba3d67e`. Final verification result is stored in `dist/installer-verification-0.7.23.json`.
- Not installed on the user's Pixel during this task. Real SMS broadcasts, background job timing under Android power management, process death, notification sound/lock screen and carrier callbacks still need on-device validation. Planning recognition includes model judgment and cannot be guaranteed in every wording or language. Quiet time is a minimum; Android scheduling and network/model latency may add delay.

## 0.7.22 Android regex compatibility and chat-load recovery — 2026-09-23

- The phone screenshot's `Syntax error in regexp pattern near index 3 (?U)\s+` was traced to readiness normalization, which every conversation read evaluates. The same unsupported inline flag also appeared in reply word counting. Replaced both with a shared precompiled explicit Unicode White_Space class. Removed the separate unsupported UNICODE_CHARACTER_CLASS API flag from request-guard initialization, retaining Unicode whitespace/digits through explicit classes. Android documents Unicode defaults and explicitly rejects that flag in its [Pattern implementation](https://android.googlesource.com/platform/prebuilts/fullsdk/sources/+/refs/heads/androidx-constraintlayout-release/android-35/java/util/regex/Pattern.java). Desktop JDK tests had accepted both constructs; they did not establish Android runtime compatibility.
- Added a reproducible ICU4C compatibility check reading the actual production whitespace/request/list expressions. It reproduced the original syntax error at index 3, then passed on ICU 78.3 with the replacement: all 25 Unicode whitespace characters, 10 nonseparators, 60/61-word boundaries, all 12 request guards, Unicode-digit essays, normal-message counterexamples and list guards. A separate read-only sweep compiled 24 remaining distinct native literal/constructed patterns without syntax errors. ICU backend verification is not a full Android runtime or phone test.
- Initial chat loading now retains the contact header and Back control. A rejected read shows Retry with collapsed escaped diagnostics instead of leaving an indefinite loading page. Retry performs one explicit conversation read and does not trigger automatic generation or sending. Thread epochs prevent late success/failure from overwriting another chat or active Settings fields; failed/superseded opens cannot apply queued-review or intent text to the wrong screen.
- Release build and all 184 Java tests passed, including four new regressions for Unicode conversation deduplication, 60/61-word limits, nonseparators and request guards without Android-unsupported flags. Lint has zero errors and nine existing warnings. Database schema, permissions, relay, model and credentials are unchanged; no migration is needed.
- Six browser suites passed: new chat-load recovery, keyboard, typing, history, reply-stop and range. Recovery covers failure→explicit retry, no automatic retry on refresh/provider events, no AI/sending on retry, stale responses during navigation and escaped diagnostics. Existing checks retain complete history, editor/cursor, reply safeguards and all four random-range controls. Failure/recovered views at 320px and keyboard-sized 412×470 were visually inspected; Back and Retry remain accessible without overflow.
- Installer verified as 0.7.22/code 32, same signer as 0.7.21, unchanged permissions, non-debuggable, private delivery/Sleep receivers, backup/cleartext off, 16 KB alignment, matching packaged assets, unchanged Tiny Blast audio and no known phone-token/API-key patterns. APK SHA-256: `0128a8c3f4398aa22c80a6a6e56760282245931982c4b5124dcb1910e45d2e31`.
- No real SMS, contacts or AI requests were used. No service deployment occurred. The update has not been installed or tested on the user's Pixel; that remains the final confirmation of on-device recovery.

## 0.7.21 Random reply-delay ranges — 2026-09-23

- Added Random range alongside existing fixed/custom delays in person profiles, Sleep, manual timers and default timer preferences. Bounds are inclusive whole minutes from 1 to 10,080; equal limits are valid, while missing, malformed, fractional, reversed and out-of-range values reject. Switching to fixed timing or Autopilot does not fail because of unfinished inactive range inputs. Existing fixed/custom values and opt-ins remain unchanged.
- Native code selects one uniformly random whole-second delay at automatic queue creation after preflight checks, or before a manual approval dialog. Manual approval shows the range and exact chosen delay, and starts that same interval only after acceptance. Automatic jobs retain chosen auto_delay and due values; refresh, notifications and dispatch never resample. Sleep holds a selected due time at or after its cutoff rather than clipping or rerolling. Saved zero-delay values cannot turn range mode into immediate Autopilot. All existing history, loop, pause, revision and recipient checks remain.
- Schema v9 adds profile auto_delay_mode/min/max with fixed-mode defaults; existing jobs need no new columns. Five SQLite checks passed against actual production migrations, including historical profile, delivery, loop, Sleep and new v8→v9 range fixtures. Current and upgraded schemas match, and profiles, drafts, opt-ins, manual timers, delivery records and silence decisions remain intact. A selected 721-second job delay and its due time survive profile changes and database reopening.
- Android release build and all 180 Java tests passed. Twelve new range tests cover strict endpoint validation, inclusive/equal bounds, seven-day limits, unchanged fixed/Autopilot timing, one-time sampling, exact manual delay text and Sleep cutoff withholding. Lint reports zero errors and nine existing warnings.
- Eight browser suites passed: new range plus send, Sleep, refinement, keyboard, typing, chat-log and reply-stop. Range checks cover all four controls, default reload persistence, reversed/malformed limits, equal/max bounds, invalid Custom/range mode switching, no implicit opt-in, raw edit retention, native-selected delay persistence, Sleep/manual timer isolation and unchanged direct sending. Fictional range controls were visually inspected at 320/412 px and a keyboard-sized 412×470 viewport with no horizontal overflow. Fixtures use fictional conversations and a simulated native bridge; typing retained cursor/focus with two saves for 62 rapid characters. The old localhost preview returned HTTP 404, so packaged assets were exercised on an isolated localhost server. Demo smoke also passed default/Sleep range persistence, one simulated selected delay, stable queued due time and preservation of the global automation-off setting.
- Installer verified as 0.7.21/code 31, same signer as 0.7.20, unchanged permissions, non-debuggable, private delivery/Sleep receivers, backup/cleartext off, 16 KB alignment, matching packaged assets, unchanged Tiny Blast audio and no known phone-token/API-key patterns. APK SHA-256: `da49aaed1eca2e5be71bc70919c51337e287534f4a713de26a19b8cd8512af98`.
- No real SMS or AI requests were made. The relay, credentials and model were unchanged. Physical Pixel installation, alarms, Doze, provider behavior and carrier timing remain untested; scheduled submission does not guarantee a matching delivery time.

## 0.7.20 Autopilot naming — 2026-09-23

- Renamed the per-person Send instantly option to Autopilot, including the profile badge, composer status, visible validation guidance and current installation instructions. Its description still explicitly says replies send as soon as ready without review. Saved instant mode identifiers, zero-delay values, eligibility, Sleep overrides and all sending logic remain unchanged. No new migration, permission, service change or pairing is needed.
- Release build and lint passed with zero errors and nine existing warnings. Existing Sleep/browser regression passed after the rename, covering mode persistence, effective delays, manual isolation and cutoff controls. The previous local preview returned HTTP 404; the same packaged assets passed on a temporary isolated localhost server. No new tests were added for this text-only change and Java logic tests were not rerun. No real SMS or AI requests were made.
- Installer verified as 0.7.20/code 30, same signer as 0.7.19, unchanged permissions, non-debuggable, private receivers, backup/cleartext off, 16 KB alignment, matching assets, unchanged Tiny Blast audio and no known phone-token/API-key patterns. APK SHA-256: `93831db602ccf042cbd4a82c1f5ddec091584d9356fa5242912b74a2dbb77489`.

## 0.7.19 Simpler timers and one-off Sleep schedule — 2026-09-23

- Unified manual, per-person and default timer choices to 1 minute, 5 minutes, 30 minutes, 1 hour and Custom. Saved nonpreset values, including 10/15 minutes, remain unchanged as Custom. Manual send-arrow/long-press behavior and the one-minute-to-seven-day custom range remain.
- Added a persistent global Sleep session with a selected delay and next local occurrence of a cutoff time. While active, its delay replaces the saved delay only for already opted-in automatic-send contacts. At cutoff all background drafting/sending pauses until explicit resume or a new session. No contact/global opt-in is enabled, no proactive periodic text is generated, and existing history/closure/repeat/five-reply limits remain. Start requires existing precise-timer access; Pause/Resume do not.
- Start/resume capture a provider SMS boundary so only subsequently received messages can start automatic work. Session changes durably increment a revision before canceling automatic jobs, preserving manual timers and in-flight manual drafts. Stale model completions and failures are quietly discarded. Due times at or after cutoff are withheld. Fresh wall and same-boot elapsed deadlines are checked before generation, queueing/activation and immediately before carrier submission, including after potentially slow provider insertion. An exact unsubmitted provider row is removed or marked unsent if the final guard stops a send. Cutoff cleanup uses a private receiver; delayed cleanup cannot bypass dispatch checks. Clock changes/reboot pause active Sleep; timezone-only changes preserve manually accepted timers.
- Schema v8 adds job sleep_revision and auto_delay snapshots without rewriting saved contact profiles. Four SQLite checks passed using real historical schemas and actual production migrations: profile, delivery, loop and v7→v8 Sleep preservation. Drafts, manual timers, opt-ins, delivery records and decisions remain intact; fresh/upgraded schemas agree; snapshots survive profile changes and reopening the database.
- Release build and all 168 Java tests passed after the final clock-sampling refinement. Fourteen new policy tests cover cutoff equality, late callbacks, paused/stale revisions, future-message boundaries, wall/elapsed/reboot expiry, next-day/midnight, DST gaps/overlaps and delay bounds. Lint has zero errors and nine existing warnings.
- Nine browser suites passed: sleep, send, refinement, keyboard, typing, chat-log, inbox-live, history and reply-stop. Checks cover legacy/custom choices, precise-timer affordances, no implicit opt-in, manual timer preservation, persisted session controls, cutoff state/focus retention, saved effective delays for instant contacts, unsaved settings retention and manual sending while paused. Browser demo persistence/expiry checks also passed. Active settings/inbox/chat screenshots at 320/412 px and a 412×470 keyboard layout were inspected; no horizontal overflow was found.
- Installer verified as 0.7.19/code 29, same signer as 0.7.18, non-debuggable, unchanged permissions, private delivery/Sleep receivers, backup/cleartext off, 16 KB alignment, matching packaged assets, original Tiny Blast audio and no known phone-token/API-key patterns. APK SHA-256: `d8770292a1ee4fc047f9f3683c3a0ae9a31755eb7bb0c0f7542fa2385b7776c7`.
- No real SMS, contacts or AI requests were used. The relay and its credentials were unchanged and not redeployed. Physical Pixel cutoff/alarm/Doze/reboot/provider behavior remains untested; browser viewport tests and pure policy tests do not establish carrier timing or delivery. Messages already submitted before cutoff may be delivered afterward.

## 0.7.18 Personal-reply safeguards and per-person chat logs — 2026-09-23

- Added firm 360-character/60-word AI output limits and code-fence/long-list rejection without truncation. Clear tutorial, coding, essay/homework and instruction-override requests return an empty `needs_review` decision. The cloud prompt requires owner input for unknown personal answers, credentials and uncertain intent, and treats imported examples as voice context rather than current facts or instructions. Contextual judgment is not a guarantee of correctness. Manual SMS composition retains its 1,600-character limit.
- Automatic modes require 20 distinct usable turns, including at least five owner and five incoming turns. The phone computes eligibility from up to 50 recent SMS plus recognized per-person samples, deduplicates normalized speaker/text and excludes exact-bound known app automatic sends from owner evidence. Fresh checks cover profile save, generation, queue/activation and carrier dispatch, including old jobs. Ineligible profiles keep manual drafting available and cannot enable automatic modes. The service withholds automatic requests missing explicit readiness, so older installed versions need the app update for automation.
- Reply setup now offers local `.txt` import and paste, explicit owner-name mapping for two-person WhatsApp exports, a checked speaker/count preview and explicit profile save. Input is bounded to 256 KB for files; selected context retains up to 50 complete turns within 8,000 characters. Unsupported/group/ambiguous logs and malformed UTF-8 fail visibly. Imported multiline labels remain quoted content rather than acquiring a different speaker on reparse. The import never writes SMS, calls AI, enables automation or sends a message. Removal recomputes eligibility. Existing samples storage is reused without a database migration or new permission.
- Release build and all 154 Java tests passed. Android lint has zero errors and nine existing warnings. Eighteen parser tests cover supported exports, explicit identity, group rejection, Unicode, quoted labels, bounded turns/characters and adversarial header whitespace. Additional policy/file tests cover readiness thresholds, deduplication, request/output holds and malformed response types. All 39 relay/HTTP fixture tests passed. The three existing SQLite migration/reply-loop checks passed.
- Nine browser suites passed: chat-log, reply-stop, keyboard, typing, send, history, inbox-live, contacts and receipts. New checks cover hidden automatic choices, staged imports, wrong owner, check-before-save, no implicit opt-in, correct-contact persistence, stale responses after navigation, removal, manual composition and needs-review display. The fictional chat-log preview screenshot was visually inspected. Existing typing, keyboard and sending interaction checks remain simulated.
- Railway deployment `8dd3365c-acef-4ead-b7f9-11f8d10ac188` succeeded with unchanged credentials/model, one replica and sleeping disabled. Authenticated health reports safety version 1. Seven fictional live checks passed: Java tutorial held, legacy missing readiness held, instruction override held, closing acknowledgment quiet, ordinary Java-class news answered briefly, unknown shift availability held, and a known current meetup time answered briefly. Only three of those required model calls. No personal conversations were uploaded and no real SMS was sent. Records are in `dist/reply-safety-live-0.7.18.json` and `dist/reply-safety-deployment-0.7.18.json`.
- Installer verified as 0.7.18/code 28, same signer as 0.7.17, non-debuggable, unchanged permissions, backup/cleartext off, private delivery receiver, 16 KB alignment, matching packaged assets, original Tiny Blast bytes and no known phone token/API-key patterns. APK SHA-256: `d2ab96bc3ac94f2a43cd0360426dfa982320c9879609ac8bd631fcd22e0b3670`.
- Physical Pixel installation, Android document-picker lifecycle, real SMS/provider changes and background/notification behavior were not exercised. Local Nano/quick replies are manual suggestions with request/output guards; they do not use the cloud's structured contextual decision or automatic dispatch.

## 0.7.17 Stop automatic-reply loops — 2026-09-23

- OpenAI now returns a strict reply/no_reply decision. Conversation-complete and repeated-reply results have an empty body and are never submitted as texts. Whole-message acknowledgment checks skip the provider after an immediately preceding sent reply; acknowledgments with new questions and incoming requests followed by a separate thanks remain available for contextual judgment. Long normalized repeats are suppressed, while short answers such as Yes/No remain possible.
- The phone saves silence per conversation and incoming message, cancels pending automatic timers, and preserves user-edited drafts. Reopening/polling does not regenerate that message. A separate persistent cap permits at most five consecutive automatic submission attempts per person until a manual Reply Pilot send is accepted or confirmed delivered. It is checked before generation, scheduling and carrier dispatch, including existing queued jobs. Explicit Draft/test requests do not automatically send.
- All 30 relay/HTTP tests passed with fictional provider responses. Seven browser suites passed: reply-stop, send, typing, inbox-live, history, contacts and receipts. The new suite verifies silence through repeated opens and provider events, fresh-message generation, manual editing/sending, quiet status changes preserving editor identity/selection, Try AI no_reply output, and the paused-cap view at a 412×470 keyboard-sized viewport. Both new screenshots were visually inspected.
- Railway deployment `02615fc7-1167-46b1-a0c2-3b6c0cfacea2` succeeded. Only the five allowlisted runtime/build files were uploaded. The existing model, endpoint, pairing and private variables were retained. Authenticated health reports `replyDecisionVersion:1` and context version 2; unauthenticated health remains denied. One replica remains running with sleeping disabled.
- Five live fictional scenarios passed: legacy Ok and emoji closures returned no_reply without a provider call; a longer closing acknowledgment returned no_reply from the model; a new question after Ok and an explicit draft request returned replies. Three model calls took 1.433–3.441 seconds in these spot checks. No real conversations, contacts or SMS were sent. This is a targeted regression check, not a guarantee that every future model decision will be correct.
- Release build and all 118 Java tests passed, including 11 new automatic-reply policy cases for the reported acknowledgment loop, substantive follow-ups, consecutive incoming messages, long duplicate replies, short valid repeated answers, meaningful punctuation, the five-submission cap, explicit silence and malformed decision handling. Lint reports zero errors and nine existing warnings.
- Actual SQLite checks passed for v6→v7 preservation/fresh-schema equivalence and persisted reply decisions/counts across reopening. They cover confirmed manual reset, uncertain/failed automatic submissions, no time-only reset, old manual callbacks, thread isolation, same-base generated-draft removal and preservation of edited/other drafts. Historical v1–v5 profile and v5→v7 delivery migrations also passed. These fixture tests do not replace an on-device upgrade check.
- Installer verified as 0.7.17/code27 with the same signer as 0.7.16, non-debuggable, unchanged permissions, backup/cleartext disabled, 16 KB alignment, matching packaged assets and unchanged Tiny Blast audio. Known phone-token bytes and API-key patterns are absent. APK SHA-256: `8340ef9255a176c9524669a8dc8cea44f89bc4478c40543dc29b9d453db2c6cc`.
- Deployment and live result records: `dist/reply-loop-deployment-0.7.17.json` and `dist/reply-decisions-live-0.7.17.json`. Physical Pixel/background notification and carrier behavior remain to be verified on device.

## 0.7.16 SMS delivery receipts — 2026-09-23

- New sends request separate per-part carrier delivery reports. The private receiver parses an explicit format/PDU status report, never a broadcast success code. Only GSM status0 or CDMA received status2 shifted16 confirms a part; every part must confirm before the whole message is Delivered. Invalid/missing reports remain uncertain. See [SmsMessage status values](https://developer.android.com/reference/android/telephony/SmsMessage#getStatus()) and [SmsManager delivery callbacks](https://developer.android.com/reference/android/telephony/SmsManager).
- Version6 adds persistent interpreted part reports, delivery status, confirmation time and exact outgoing SMS date. Part updates and aggregates are transactional, survive restart and handle duplicates, out-of-order temporary states and late proof. Delivery does not change send authorization, trigger a retry or sound another notification. If full proof arrives before the existing sent-result notification, its wording retains that confirmation.
- Provider writes and private receipt annotation require the exact URI, thread, address, body and captured SMS date, preventing a delayed callback from updating a deleted/reused row. Independent review found an existing sent-result URI-only write; it now uses the same binding. Legacy jobs lacking a date keep their Queue outcomes but skip ambiguous provider mutations. Old jobs start with no new delivery claim. Actual v5 schema/v6 migration SQL tests preserved jobs, draft text, profiles and prior submission receipts and matched the fresh-install schema.
- Chat, inbox and Queue distinguish Sent, Delivered and Delivery failed; local read flags never imply remote reading. A bounded, thread-scoped query refreshes receipt fields of older currently visible messages, without fetching all history or making per-row lookups. Receipt-only changes redraw the timeline while retaining editor identity, selection and scroll anchor. No unsupported RCS/read toggle is offered; Settings explains capability limits.
- All six browser suites passed: receipts, sending, live inbox, full history, contacts and typing. Focused checks included absent/malformed/provider-only receipt data, authoritative delivery proof, multipart pending fixtures, duplicate updates without dispatch, receipt-only live updates, visible older cached message updates and 412/320 px layout. Both receipt screenshots in dist were inspected. Browser fixtures test display behavior; native aggregation is covered separately.
- Release build and all 107 Java tests passed, including 16 delivery-policy cases for protocol statuses, multipart completion, late/duplicate reports, restart reconstruction, invalid parts, recycled-ID binding and delivery-before-sent wording. Lint has zero errors and nine existing warnings. No real SMS/PDU callbacks, personal messages, AI requests, notification or service deployment were used. Actual Pixel/carrier delivery reports remain untested; support depends on the carrier.
- RCS feasibility was checked against current official sources. The [IMS single-registration interface](https://source.android.com/docs/core/connect/ims-single-registration) requires a privileged preinstalled/default SMS app with carrier certification. The newer [AlternativeMessageTransportService](https://developer.android.com/reference/android/service/messaging/AlternativeMessageTransportService) delegates to the default app’s own transport; it does not give this APK access to Google Messages/Jibe. RCS remains unimplemented, and SMS cannot supply read receipts.
- Installer verified as 0.7.16/code26, same signer as 0.7.15, non-debuggable, unchanged permissions, backup/cleartext off, 16 KB alignment, matching packaged assets and original Tiny Blast audio. The delivery receiver is non-exported. Known phone token and API-key patterns are absent. APK SHA-256: `7a2b28bd4a403b0cab760c71806abfbfe3dd47de68c367b24ae2649e955345a6`.

## 0.7.15 contact picker — 2026-09-23

- New Message opens a dedicated contact picker with a compact name/number search, labeled number choices, manual-number fallback, loading/error/empty states and a bounded-results hint. Searches wait for a 180 ms typing pause and update the results without replacing the input or cursor. Choosing a number captures the intended recipient, flushes edited drafts, locks duplicate actions, and opens the existing composer; stale responses and results after navigation cannot open a different page.
- Contacts can be allowed independently of the SMS role, from the picker or Settings. Android denial leaves manual number entry available; blocked prompts offer this app’s system settings. Permission results wait for foreground resume when needed before continuing. Revocation clears cached choices and invalidates older reads. Contact choices stay in process memory only and are cleared when the picker closes; no search data is uploaded.
- The native worker queries local phone rows only, by encoded name/number filter, sorts them by name and retains at most 80 unique sendable choices plus lookahead. Multiple labeled numbers remain distinct; duplicate aliases for the same contact are removed. Extensions, vanity/service strings and invalid addresses are not silently converted. Permission is checked before/after the query and at main-thread bridge delivery. READ_CONTACTS was already declared; no new manifest permissions or exported components were added.
- Focused browser regression passed for allowed/denied/blocked/revoked permission, manual numbers, delayed and stale searches, debounce, active query/caret preservation, escaped contact names/labels, empty/error/retry, multiple numbers, failed/late compose responses, busy and duplicate selection guards, original draft retention and narrow layout. The initial visual check found an inherited flex rule stretching search; this was corrected and the final 52 px height assertion passed. The clean 412 × 915 screenshot at dist/contacts-picker-preview.png was inspected. Inbox-live, send, Media, typing and refinement suites passed.
- Release build and all 91 Java tests passed, including eight contact-policy cases for formatting, invalid numbers, per-contact deduplication, distinct labeled/shared numbers, real lookahead, bounded literal queries and missing names. Lint reports zero errors and nine existing warnings. No real contacts, SMS, AI request or service deployment was used. Actual Pixel permission prompts and contact-provider behavior remain on-device checks.
- Installer verified as 0.7.15/code 25, same signer as 0.7.14, non-debuggable, unchanged permissions, backup/cleartext off, 16 KB alignment, matching packaged assets and original Tiny Blast audio. Known phone token and API-key patterns are absent. APK SHA-256: `13aaa71ca24f94617986aa1f500a1aafa7de5e905181f532e460229168302fe2`.

## 0.7.14 live inbox updates — 2026-09-23

- While the Activity is foregrounded, an SMS provider observer and asynchronous process-local send-status signals notify the page without message contents. A fixed 50 ms window coalesces bursts. Dispatch/receipt signals also cover status changes when no provider write succeeds. Pause/destroy removes the subscription, observer and pending callback; permission results recheck registration. Existing resume refresh and polling remain fallbacks.
- The frontend queues a fresh snapshot after any older in-flight read, coalesces subsequent events and retains updates received during busy operations. Inbox rows and queue counts update without rebuilding the search input. A visible-row anchor preserves list position; focused row controls and search focus, selection and text survive. Outgoing outbox, failed and queued provider rows show You previews with honest status labels.
- The focused live-inbox browser suite passed with periodic polling disabled and fictional provider data: scheduled to sending to sent previews, timestamps, order and queue badges; focused search/caret and scrolled anchor; 100-event coalescing during a held stale snapshot; failed/waiting labels; retained typed replies; 20 events during a busy operation; and navigation/Settings focus during late completion. Existing typing, Media, send, full-history and refinement suites passed.
- Release build and all 83 Java tests passed, including seven process-local event tests for asynchronous delivery, unsubscribe/resubscribe, bursts, changes during delivery, concurrent publishers and rejected executors. Lint has zero errors and the same nine warnings. No real SMS, personal messages, live AI requests, notification or service deployment was used. Actual Pixel/provider/carrier timing remains to be verified on-device.
- Installer verified as 0.7.14/code 24, same signer as 0.7.13, non-debuggable, unchanged permissions, backup/cleartext off, 16 KB alignment, matching packaged assets and exact original Tiny Blast audio. Known phone token and API-key patterns are absent. APK SHA-256: `43ec48541644d175cbf6a6453e057035e7220034cc33650557b99eedf4f5009d`.

## 0.7.13 search focus and immediate Media navigation — 2026-09-23

- Search uses one focus ring on its pill. Tapping outside, Enter/Escape, route changes and leaving the app blur only search while preserving its query. Native IME visible-to-hidden transitions notify the page after layout; a stale callback is ignored if the keyboard reopens. This does not blur the reply editor or change native inset geometry.
- Media renders before waiting for native attachment reads or pending draft saves. A small animated paper plane/dotted flight path marks loading; reduced motion keeps it still. Loading, empty and error/retry states are distinct. Successful results remain in process memory for 30 seconds for quick returns. Refresh re-reads and retries photo frames. Duplicate requests share the in-flight operation; late results never navigate away or replace another page's editor.
- Photo elements use lazy loading and asynchronous decoding, with placeholders and clear image-error feedback. Media cards do not rebuild for ordinary background snapshots or unchanged background refresh results. Media avoids unrelated chat-history polling while open. The native loader resolves each sender name once per response, rechecks contact permission and drops the cache afterward; the existing latest-60 MMS limit and streaming image route are unchanged.
- Focused media/search regression passed with delayed fictional native reads and saves, immediate destination visibility, loading/empty/error/retry, cache/duplicate requests, escaped content, lazy images, navigation-away results and search query/focus behavior. The keyboard-hide callback preserves a focused reply editor. Phone and desktop loading screenshots were inspected, including 320/412 px overflow checks.
- Existing keyboard, typing, smoke and touch-send suites passed. Release build and all 76 Java tests passed; lint has zero errors and the same nine existing warnings. These are browser/compiled checks; physical Pixel keyboard dismissal and provider/image-loading timings remain unmeasured. No real messages/media, SMS sends, live AI request or service deployment were used.
- Installer verified as 0.7.13/code 23, same signer as 0.7.12, non-debuggable, unchanged permissions, backup/cleartext off, 16 KB alignment, matching packaged assets and exact original Tiny Blast audio. Known phone token and API-key patterns are absent. APK SHA-256: `1e3f419c70c9f174902644fb47d0e7f91701dad7076d8c401e3b5f52891413b0`.

## 0.7.12 tap to send and hold to schedule — 2026-09-23

- The filled send arrow dispatches the exact displayed reply immediately on a short tap. A 500 ms hold opens the existing timer menu; its release cannot dispatch a message. Moving over 12 px, pointer cancellation, app pause, blur and hidden-page changes cancel the hold. Release-click suppression remains latched until a fresh pointer gesture, including prolonged drags/holds. The visible Schedule send shortcut, right-click and Shift+F10/Down Arrow expose timers accessibly; Enter/Space activate immediate sending.
- Immediate sends lock before awaiting draft saves and capture thread, base, recipient, SIM and exact body. Native sendNow has a fixed zero delay and returns the persisted send status. Only sending/sent acceptance clears the draft. Rejected/paused/uncertain outcomes preserve text; the native layer refuses duplicate/uncertain submissions. Delayed manual timers retain their separate confirmation and existing options/bounds.
- Native validation checks role, READ_SMS/SEND_SMS, active SIM, recipient-thread consistency, latest message and pending jobs. Immediate sending does not require exact-alarm access. The Activity rejects full/partially obscured touches before the WebView. A late cloud result cannot save or automatically send a second reply for a contact/message base already submitted to the carrier; unrelated contacts and retryable pre-carrier failures remain unaffected.
- Release build and all 76 Java tests passed, including seven added policy cases for instant timing, conversation bounds, duplicate/uncertain sends and late generation isolation. Lint passed with zero errors and the same nine existing warnings.
- The focused send suite passed: exact-body quick send without dialog/delay, real Chrome touch input, prolonged hold/release, drag-return and cancellation, blank/pending guards, keyboard activation, all timer choices, duplicate activation during saves/sends, failed-save/carrier/paused-result retention and explicit retry. All bridge responses were synthetic; no carrier call occurred.
- Existing browser typing, keyboard, refinement, safety, smoke, instant-mode and full-history suites passed against fictional data. The send-arrow and timer-menu screenshots in dist/send-now-preview.png and dist/send-hold-preview.png were visually inspected. Physical Pixel gestures, overlays and carrier delivery remain untested. No real SMS, AI request, notification or service deployment was made.
- Installer verified as 0.7.12/code 22, same signer as 0.7.11, non-debuggable, unchanged permissions, backup/cleartext off, 16 KB alignment, matching packaged assets and exact original Tiny Blast audio. Known phone token and API-key patterns are absent. APK SHA-256: `10b1f056c4afb50a508ae5a02487e8d711f805c214b5d424aba7b4ec5a7721ec`.

## 0.7.11 distinct chat header and reply surfaces — 2026-09-23

- Contact header and reply composer use a stronger theme-tinted surface with thin dividers. The timeline uses each theme's deeper background. The rounded input and reply-setup control have their own contrasting surface. All ten palettes use existing theme tokens; focus, text sizing, keyboard layout, scroll behavior and sending logic are retained.
- Existing keyboard regression passed: stale visual height/offset, native-first and visual-only resize, repeated open/close, narrow long drafts, editor identity, focus and cursor. These are simulated viewport transitions; physical Pixel keyboard rendering remains untested.
- Theme review passed at 320 px and 412 px across chat, profile, timer, settings, inbox and AI preview screens: 5,190 visible text/placeholder samples, minimum contrast 4.66:1, no horizontal overflow or page errors. Theme choices persisted across reload. Normal and keyboard-sized Midnight screenshots were visually inspected; chat-surfaces-preview.png and chat-surfaces-keyboard-preview.png are in dist/.
- Release build and lint passed with zero errors and the same nine existing warnings. The packaged assets match source. No real SMS, AI request, notification or service deployment was made.
- Installer verified as 0.7.11/code 21, same signer as 0.7.10, non-debuggable, unchanged permissions, backup/cleartext off, 16 KB alignment, exact original Tiny Blast audio and no known phone token/API-key patterns. APK SHA-256: `52f9eb9193421abd346a17bc874e2906a2284cffbbfa4c89f1c60df70db4e87f`.

## 0.7.10 adaptive launcher icon — 2026-09-23

- Replaced the launcher artwork with a midnight gradient background, blue/violet message bubble, white paper plane and cyan spark. Separate native vector background/foreground layers use the 108 dp canvas and centered safe area described in [Android adaptive icon guidance](https://developer.android.com/develop/ui/compose/system/icon_design_adaptive). A separate monochrome layer supports themed icons. The icon and roundIcon manifest fields share the adaptive resource.
- Notifications retain their existing resource ID but now use a transparent white message/plane glyph, appropriate for status-bar tinting. Notification behavior, channels, Tiny Blast audio, pairing and permissions are unchanged.
- SVG previews generated from the native vector paths were rendered and visually inspected with circular, rounded and themed masks, including 52 px home-screen samples. Preview files are in dist/icon-preview.png and dist/app-icon-preview.png; editable SVG counterparts are in design/. Physical Pixel launcher rendering remains untested.
- A clean release build and Android lint passed with zero errors and the same nine existing warnings. Packaged resource inspection confirmed full-color and monochrome adaptive layers, both manifest launcher references, and the notification glyph. No business logic changed and no real SMS, notification, AI request or service deployment was made.
- Installer verified as 0.7.10/code 20, same signer as 0.7.9, non-debuggable, unchanged permissions, backup/cleartext off, 16 KB alignment, matching packaged assets and unchanged original Tiny Blast audio. Known phone token and API-key patterns are absent. APK SHA-256: `71f67b03de8714f755d77dc1c1fe0e8aa9b957d7e6b6feafdf3317e0700b64c3`.

## 0.7.9 complete SMS history and recent-50 reply context — 2026-09-23

- Chats now load SMS in 50-message pages with no total cutoff, using thread-scoped exclusive date/ID cursors and one lookahead row. Reading upwards preserves the visible message, reply field and cursor. Failed reads offer retry. Loaded pages survive refresh, redrafting and new arrivals. Disjoint newest-page windows fill intervening messages with a visible gap/retry marker; authoritative empty results clear cached SMS and invalidate pending reads.
- After SMS access, app resume prepares recent context locally for AI-enabled contacts when history matching is on. Chat opening refreshes that context; disabling matching or a contact excludes its preparation. A bounded process-only cache stores no separate model or permanent training data. Every actual OpenAI draft refreshes the latest 50 valid, nonblank incoming/sent messages for that same thread. Queued, failed, unsent and other-contact messages are excluded.
- OpenAI receives up to 50 combined turns plus up to six focused sent examples from within that window. Older turns are capped at 600 characters, while the incoming message remains intact within the existing 1,600-character limit. Full displayed SMS text is not shortened. Matching off keeps eight immediate turns and explicit samples. Optional local AI context limits are unchanged. Prompt examples follow [OpenAI context guidance](https://developers.openai.com/api/docs/guides/prompt-engineering); no fine-tuning is performed.
- Relay accepts the larger bounded context with a 256 KiB HTTP/native request cap, validates the matching flag, and reports context version 2/limit 50. Old phone versions retain the preference inferred from their style examples. Existing authentication, response storage off, fixed model endpoint, request deduplication, output/refusal checks and rate limits remain.
- All 69 Java tests and 18 service tests passed. New checks cover complete traversal of 267 messages, timestamp ties, cursor boundaries, concurrent arrivals, context scope, latest-50 filtering, opt-out behavior, body size and old-client compatibility. Release build and lint passed, with zero errors and nine existing warnings.
- Browser history, typing, keyboard, refinement, instant-send and safety suites passed using simulated phone data. History checks traverse 173 messages and bridge a 100-message background burst, cover retry, duplicate/stale responses, empty-result clearing and restored access. Rapid typing still used two saves for 62 characters. Screenshots of older-message loading and the 50-message/25-sent setup note were inspected.
- The hosted service was updated without changing credentials, pairing, endpoint or model. An authenticated fictional 50-turn request returned a valid reply from gpt-6-sol in 2.081 seconds. No personal messages were uploaded and no real SMS was sent. Service deployment and installer verification records are stored in dist/.
- This shows SMS available through Android's SMS provider; it cannot recover deleted messages or Google Messages-only RCS data. MMS remains in Media. Native provider performance, on-device permissions, full Pixel history and keyboard behavior still require phone confirmation. Existing notification sound, themes, native insets, timer policy and contact opt-ins are retained.

- Installer verified as 0.7.9/code 19, same signer as 0.7.8, non-debuggable, unchanged permissions, backup/cleartext off, 16 KB alignment, matching packaged assets and unchanged Tiny Blast audio; known phone token and API-key patterns absent. APK SHA-256: `0bd4659ec7e99c3cf7df88a4b46cc324a0ae96a8bf39c5ed8b6b3c9b8d41058d`. Final Railway deployment `649ff85a-5c3c-414e-b87b-e6f6077ee691` is healthy with the existing phone credential.

## 0.7.8 deeper color themes — 2026-09-23

- Replaced all ten pale/subtle palettes with distinct rich dark colors throughout backgrounds, cards, incoming/outgoing bubbles, navigation and controls. Outgoing bubbles and filled actions have their own readable foreground color. Theme cards preview each actual palette; form placeholders and selections use theme colors.
- Midnight remains the default, with the existing one-time migration marker retained. Saved theme choices and unrelated preferences are preserved. Native window/background colors match all ten CSS palettes; status/navigation icons remain light. The launch surface and browser theme color match the deeper Midnight palette.
- Browser review covered all ten themes across Settings, chat, reply profile, timer, inbox/new-message and Try AI at 320px and 412px. All ten persisted across reload. 5,110 actual computed text/placeholder contrast samples passed at 4.66:1 or above; no horizontal overflow or page errors. Theme picker and Forest/Ocean/Midnight chat screenshots were inspected. No additional visual effects were added to typing or scrolling.
- Existing refinement and keyboard regressions passed, including themes, saved preferences, reply setup, timer/editing guards, viewport transitions, long replies, focus and cursor preservation. These tests use synthetic phone data and viewport behavior; physical Pixel rendering still needs confirmation.
- Release build and lint passed, with zero errors and the existing nine warnings. No real SMS, notification, live AI request or service deployment was made. The Tiny Blast audio and notification behavior, pairing, permissions, sending policy and keyboard layout are retained.

- Installer verified as 0.7.8/code 18, same signer as 0.7.7, non-debuggable, unchanged permissions, backup/cleartext off, 16 KB alignment, source-matching assets, exact original Tiny Blast audio and no known phone token/API-key patterns. APK SHA-256: `70b693b022b75ccfff1958ae6da64bb7268c6910f990bb3bfc85e1eb20232d68`.

## 0.7.7 Tiny Blast notification sound — 2026-09-23

- Bundled the supplied MP3 unchanged (33,811 bytes; SHA-256 `f3ee786024c9d2aa56b5f9f552d8c98f0398ba081265f9d4602123b2d7b1a084`). Chrome decoded it successfully as stereo 44.1 kHz audio, about 2.01 seconds excluding MP3 padding. No audio was played or uploaded. The APK resource table resolves raw/tiny_blast to audio identical to the original.
- Incoming-message and draft/timer categories use Tiny Blast for new installs and unchanged default channels. The sound URI uses the resource type/name, not its numeric ID, with an explicit R.raw reference to retain the asset during optimization.
- Android channel sound settings are immutable after creation. The migration selects new stable channel IDs only for original defaults and persists that choice. Customized, blocked, silent, privacy-adjusted, vibration, badge or DND settings keep the existing channel itself. Later user changes are never reset. No old channels are deleted or active notifications reposted. See [Android channel guidance](https://developer.android.com/develop/ui/compose/notifications/channels) and [NotificationChannel reference](https://developer.android.com/reference/android/app/NotificationChannel).
- Old and new draft channels remain recognized for cancellation/privacy/status updates; existing updates keep their original channel and OnlyAlertOnce behavior. App/channel permission checks and the requirement to post an alert before automatic sending remain. Settings opens the selected draft channel.
- Release build and all 60 Java tests passed, including eight new channel-selection tests covering fresh install, untouched/customized legacy settings, retained choices, muted new channels, missing channels, interrupted preference writes and invalid saved IDs. Phone-setup browser regression passed. Lint reports zero errors and the same nine warnings. Platform channel introspection and actual playback still need Pixel confirmation.
- Installer verified as 0.7.7/code 17, same signer as 0.7.6, non-debuggable, unchanged permissions, backup/cleartext off, 16 KB alignment, source-matching assets, exact original audio, no known phone token/API-key patterns. APK SHA-256: `a27c02fd44ff1ba5337e5b0e2bf4643e3bd5581127ca56d765093d217713ee9c`.
- No real SMS, notification or live AI request was sent. No hosted service, model, pairing, theme or keyboard changes. Android volume, Do Not Disturb and existing user sound settings still apply.

## 0.7.6 keyboard-visible composer — 2026-09-23

- The user confirmed the screenshot came from 0.7.2, before native safe-area containment. The 0.7.6 installer includes all later header, status-bar and Midnight changes and can update 0.7.2 directly.
- Reproduced a remaining stale-metric failure in the browser: with a 560px layout and an old 915px visual viewport, the composer was placed outside the visible area. The chat now uses the smaller valid layout/visual height and clamps the visual offset to that space. Resize work is coalesced per frame, also refreshed on focus, foreground visibility and native resume, without replacing the active editor.
- The native container still handles status bars, cutouts and keyboard space once, but now forwards a copied WindowInsets with the handled dimensions set to zero. This lets WebView receive keyboard transition updates instead of suppressing them with CONSUMED. Based on [Android's WebView inset guidance](https://developer.android.com/develop/ui/views/layout/webapps/understand-window-insets).
- Removed the chat shell's full-screen minimum height and disabled outer-page scrolling in phone chat mode. The history remains scrollable, and long reply text is capped to the reduced viewport and scrolls inside the field.
- The new keyboard regression passed: stale visual height and offset, native and visual-only resize, repeated open/close, narrow long replies, focused editor identity and cursor preservation. Existing typing and refinement suites also passed, including two writes for 62 rapid characters, timer cancellation, draft saves, preferences and phone layout. The reduced-height screenshot was inspected. These are simulated viewport transitions, not a real Android keyboard test.
- Release build and lint passed: zero errors, nine existing warnings. Installer verified as 0.7.6/code 16, same certificate as 0.7.5, non-debuggable, unchanged permissions, backup/cleartext off, 16 KB ZIP alignment, source-matching assets, known phone token/API-key patterns absent. APK SHA-256: `dff41ae12f99edf8d502ef16b4a62697fc3327115374cc07705686f8f525a292`.
- No real SMS or live AI request was made. Sending policy, pairing, service and contact profiles are unchanged. Physical Pixel/Gboard keyboard behavior still needs confirmation after checking Settings shows 0.7.6.

## 0.7.5 Midnight default — 2026-09-23

- Restored the original navy Midnight palette and its Settings label from 0.7.2. Midnight remains the default and fallback. Native launch/window/status surfaces match the navy background. All ten themes and the 0.7.4 header/inset layout remain available.
- One-time phone/browser migration selects Midnight on first open of this update. Later theme changes remain saved. Browser checks verify the default palette/label, ten legible theme choices, old-install migration, preservation of unrelated preferences and retention of a subsequent theme choice. Inspected the updated inbox screenshot.
- Release build and lint passed: zero errors, nine existing warnings. No sending logic, model/service, credentials, permissions or contact profiles changed. No live AI or real SMS request was made. Native phone appearance still needs device confirmation.
- Installer verified as 0.7.5/code 15, same signer as 0.7.4, non-debuggable, unchanged permissions, backup/cleartext off, 16 KB ZIP alignment, source-matching assets and no known phone credential/API-key patterns. APK SHA-256: `6057f97f0f8dee7b8a24cc659cd2b99cf94a445ca73bd11110ce2e4680bc8003`.

## 0.7.4 native safe area and inbox header — 2026-09-23

- The reported phone screenshot showed the search field and compose button under Android's status icons. Replaced WebView padding with a native FrameLayout that reserves the union of system bars, display cutout and keyboard insets. The WebView is measured inside that space, and the parent consumes the insets once. Explicit edge-to-edge setup and initial inset dispatch make this consistent across supported Android versions. The existing adjustResize mode and visualViewport handling remain.
- Native window/wrapper backgrounds follow the selected theme, with transparent status/navigation bars on Android 12–14 and existing light/dark icon handling. No fixed status-bar height or fake clock was added. Guidance: [Android edge-to-edge](https://developer.android.com/develop/ui/views/layout/edge-to-edge) and [keyboard insets](https://developer.android.com/develop/ui/views/layout/sw-keyboard).
- Added a compact 56-pixel Messages header with the new-message control, then search beneath it. Removed the duplicate Messages list label. Scheduled count, compose form, search, themes and existing reply controls remain functional. Inspected 320px and 412px screenshots; synthetic scheduled-count checks at 320, 412, 840 and 1024px found no title/control overlap or horizontal overflow.
- Seven relevant browser suites passed: refinement, typing, smoke, setup, style, instant and safety. Header checks confirm title/search/compose order and recipient-form access. Typing still used two draft writes for 62 rapid characters and retained cursor/IME behavior. Scoped test navigation selectors to the Main navigation landmark so the Messages theme button is not mistaken for the Messages tab.
- Release build passed; Android lint has zero errors and the same nine warnings. APK verified as 0.7.4/code 14, same signer as 0.7.3, non-debuggable, unchanged permissions, backup/cleartext off, 16 KB ZIP alignment, source-matching assets, and no known phone credential/API-key patterns. SHA-256: `7c01a605e600b41e081973b101f580197ad509c6ac04b37a0d0eed8ebd77d79a`.
- No real SMS or live AI request was made. No sending policy, service, pairing or profile data changed. A selected 0.7.3 theme is retained; the earlier one-time dark-theme migration still applies when upgrading directly from 0.7.2.
- Native Android window inset delivery cannot be proven by browser screenshots. No emulator/device run was available. On the Pixel, verify status-bar separation, camera-cutout/rotation/fold behavior, keyboard show/hide, and the reply composer before relying on the layout fix.

## 0.7.3 Messages dark palette — 2026-09-23

- Replaced the Midnight palette with Messages: charcoal backgrounds, subdued blue accents/outgoing bubbles, gray incoming bubbles and softer text. All ten palette choices remain available. Theme cards stay dark while previewing each optional palette.
- The phone and browser select Messages once on first open after this update. Native migration changes only the theme and its migration marker. Browser checks confirm the old theme migrates while tone, automation, delay, SIM and lock-screen preferences are preserved, and subsequent theme choices persist across reloads. Pairing/profile storage is untouched.
- The native launch background, window and app-owned dialogs use a dark Material theme. Window and WebView backgrounds agree with the selected palette; system-bar icon appearance remains theme-aware. Actual Pixel splash and native dialog appearance still require device confirmation.
- The existing browser refinement suite passed, including ten saved themes, theme-label contrast, phone/keyboard layouts, reply setup and existing timer/editing guards. Its viewport measurement now waits for resize and frame-scheduled sizing before asserting composer bounds, fixing a premature measurement. Inbox, chat and settings screenshots were visually inspected. The separate default/migration/persistence check passed with synthetic data.
- Release build succeeded. Android lint reports zero errors and the same nine warnings. No SMS, paid AI request or service deployment was performed. Sending logic, model, relay, permissions, signing identity and contact profiles were not changed.
- Installer verified as version 0.7.3/code 13, signed by the same certificate as 0.7.2, non-debuggable, unchanged permissions, backup/cleartext off, 16 KB ZIP alignment, and source-matching assets. Known phone credential/API-key patterns are absent. APK SHA-256: `a570079fbfe74413a4c15adebfd3ccd04892ce3c512a458ceba9c93a8b1605a5`.

## 0.7.2 typing responsiveness and rounded UI — 2026-09-23

- Native freshness checks fetch only the newest SMS ID instead of loading and serializing up to 60 messages for every draft edit. Conversation refresh uses its already-loaded history for its base ID, and read-status writes target unread/unseen rows only. No authorization check was removed.
- The first edit is saved immediately, including cancellation of an automatic timer. Subsequent input is coalesced for 220 ms. Navigation, approval, redrafting, focus loss and app pause flush current text. Failed saves prevent dependent approval/redrafting and retain the local edit. Stale-base writes cannot overwrite a fresh incoming-message draft.
- Unrelated inbox/job updates no longer replace the active chat editor. Carrier status updates patch message history without replacing the input. Overlapping polls are coalesced, old conversation responses cannot take over a newer selected thread, and native field sizing handles composer growth where supported, with a frame-scheduled fallback.
- The new synthetic typing regression passed: 62 rapidly typed characters used two draft writes. It verifies editor identity/cursor and IME preservation, immediate automatic-timer cancellation, async pause/navigation flush, contact isolation, exact approval payload, failed saves, stale-base rejection and sending-state disabling. This is a work-reduction/behavior measurement, not a Pixel frame-time benchmark.
- All eleven browser suites passed (ten existing plus typing). Phone/dark-theme/320px/keyboard-height screenshots were inspected. Rounded controls and bubbles, system font and larger text replace the sharper visual style. Current runtime assets contain no “you have the final say” banner. An initial resize assertion needed a settled viewport before its repeat; final 320×640 and 412×430 composer checks pass.
- Signed release compilation and all 52 Java tests passed with zero failures/errors. Android lint reports zero errors and the same nine documented warnings. No model, relay, schema, permission, signing identity or user-profile default changed. No real SMS or live AI request was made.
- Lifecycle change: only saveDraft may pass the native foreground gate while the activity is pausing, allowing an already-entered edit to persist and stop an automatic timer. Sending, approval and settings actions still require foreground access. Draft freshness and active-job checks are unchanged. Actual Android keyboard latency and lifecycle delivery must be confirmed on the Pixel.

- Installer verified as 0.7.2/code 12, same certificate as 0.7.1, non-debuggable, unchanged permissions, backup/cleartext off, 16 KB ZIP alignment, current source-matching assets and banner absent. Known phone credential/API-key patterns are absent. APK SHA-256: `5afff50fd48a20489f0b7e5b1da67246bd054c1382bcb87e71253ddf3f9e9c0f`.

## 0.7.1 instant sending — 2026-09-23

Contact-specific Send instantly stores the existing auto-send opt-in with delay zero; no schema change is needed and five minutes remains the default. New and upgraded profiles do not opt into instant mode. Fresh replies pass all normal authorization, notification, freshness, recipient and SIM checks. Zero-delay activation uses direct guarded dispatch after the notification posts, without exact-alarm access. Delayed sends still require timers. Notifications retain original/reply content and update the carrier outcome silently.

- Signed release build and all 52 Java unit tests passed with zero failures/errors. Lint has zero errors and the same nine documented warnings. Four new policy tests exercise instant mode without alarm access, unchanged standing authorization/freshness checks, invalid-delay rejection and carrier/duplicate-send guards.
- All ten browser suites passed. The new instant suite verifies per-person isolation, explicit zero-delay persistence, missing/null defaults, rejection of custom zero, delayed-mode fallback, no profile-save sending, global pause, Try AI isolation, outgoing insertion, composer clearing and receipt-state refresh. Existing nine suites pass unchanged. Synthetic screenshots were inspected.
- Existing SQLite migration/preservation checks pass; no new schema is required. Existing delays remain unchanged and new profiles retain auto-send off/five-minute defaults.
- Installer verification passed: version 0.7.1/code 11, same signature as 0.7.0, non-debuggable, unchanged permissions, backups and cleartext disabled, cancel receiver nonexported, 16 KB alignment, packaged assets equal source, known phone credential and API-key patterns absent. APK SHA-256: `150e269e4bf413f5996324ff25bcad2d9d2f02fdf3258f4f840a75acc7f3a7ad`.
- No real SMS or live AI request was made. No relay changes or new pairing are required. Real Pixel notification/receipt behavior, zero-delay carrier dispatch and behavior without alarm permission remain device acceptance tests, not claims established by the computer checks.

## 0.7.0 UI, automatic timers and notifications — 2026-09-23

- Release compilation and 48 unit tests passed with zero failures/errors. Android lint reports zero errors and nine warnings: eight established tool/dependency/ABI/WebView/telephony warnings plus a static-context warning. Store's private constructor is called only with `getApplicationContext()`; it does not retain an Activity. No broad dependency upgrades were made for this UI change.
- All nine browser suites passed using synthetic conversations and explicit native-bridge mocks. New coverage checks search-first layout, ten themes and persistence, partial theme settings updates, dropdown drafts/custom delays, saved-profile opt-in without scheduling old drafts, auto timer display, editing cancellation, manual confirmation rejection, saved drafts after scheduling failure, and disabled editing during manual scheduled/carrier sending states. Existing regression, setup, relationship, cloud, style, Nano and hostile-text tests also pass.
- Phone layouts at 412×915 and 320×640 and a 412×430 keyboard-height simulation were checked for overflow and composer visibility. Screenshots of the inbox, chat, settings, profile and timer were inspected. These browser previews do not show native Android notifications or a real software keyboard.
- Production SQLite statements passed fresh v5 and upgrades from versions 1, 2, 3 and 4. Existing notes, samples, opt-ins, tones, exact manual jobs and drafts survive. New per-person auto-send defaults off, delay defaults to five minutes, and per-person settings remain isolated.
- Automatic jobs use separate standing authorization and default off, capture profile/config revisions and the latest incoming SMS, require one canonical SMS recipient and a usable SIM, and recheck policy at dispatch. The original message is persisted with the exact reply. An internal awaiting-alert reservation cannot send; the notification must post before the alarm is armed. Reconciliation pauses interrupted reservations. Global pauses also invalidate in-flight model results.
- New incoming SMS/MMS and recovery events remove stale timer notices. Editing/redrafting, changed profile/SIM/connection, global automation off, revoked notification access, stale message/body, missed timers and lost SMS role pause automatic sends. Manual scheduled replies require cancellation before editing. Claim-before-carrier logic and no automatic retry remain.
- A separate notification channel uses the default system notification sound. BigTextStyle shows incoming/reply sections, countdown and an immutable Cancel action to a nonexported receiver. Lock-screen visibility follows the saved preview preference. Unchanged refreshes do not repost alerts, and dismissed notifications are not recreated. Android controls DND, display size and visibility.
- No new runtime permissions, API-key requirement or relay deployment. Final installer checks passed: version 0.7.0/code 10, same signer as 0.6.2, non-debuggable, backup/cleartext disabled, CancelSendReceiver nonexported, unchanged permissions, 16 KB ZIP alignment, source-matching assets and no known phone token/API-key patterns in distributed artifacts. APK SHA-256: `6f81c3b17bb59bcaaa8e85c26e904172366b4417809bdfd517de34e13cb55576`. Installer ZIP extraction matches the APK.
- All 14 existing relay tests passed with mocked responses; no hosted service changes or live requests were needed. The dark theme picker also passed text-contrast checks for all ten choices.
- No real SMS, paid OpenAI request or private conversation was used for this update. Actual upgrade lifecycle, canonical SMS provider queries, notification sound/rendering, software keyboard, alarms, background process death and carrier outcomes still require Pixel testing. See the acceptance checks below.

## 0.6.2 security review — 2026-09-23

- Non-debuggable signed release build passed with 39 unit tests, zero failures/errors and lint with zero errors/eight warnings. Three additional newer-version notices come from the explicit patched dependency overrides; the other warnings cover tool/dependency age, ARM-only support, packaged WebView JavaScript and telephony hardware.
- OSV matched five published advisories to Guava 31.0.1-jre, OkHttp 3.0.0 and Okio 1.6.0 in the baseline dependency graph. These are affected-version findings; exploitability through the app was not established. The release resolves Guava 33.4.8-android, OkHttp 4.12.0 and Okio 3.6.0. All 74 resolved runtime artifacts were rechecked: zero matching advisories at scan time.
- The compiled release manifest has debugging/test-only mode off, backups off and cleartext off. WebView debugging is explicitly disabled. The approval dialog rejects fully/partially obscured touch events; device overlay and ordinary touch behavior remain untested.
- All 14 mocked relay tests and all eight browser scripts passed. The new ui-security script tests hostile contact names, SMS/MMS text, profiles, draft/queue content and AI output. It found no injected elements, external requests or unintended send/save calls. The native bridge was simulated; no real Android dialog or carrier was exercised.
- Six read-only live HTTPS checks passed: public status, unauthenticated/wrong-token denial, authenticated health, browser-Origin rejection and absent send route. No OpenAI draft or SMS was generated. No relay deployment/configuration was changed.
- Release APK signature matches 0.6.1, version is 0.6.2/code 9, permissions are unchanged, assets match source, known phone token/API-key patterns are absent, and 16 KB ZIP alignment passes. The old private development certificate is retained for update compatibility; the APK itself is non-debuggable.
- See SECURITY-AUDIT.md for scope, findings, evidence, residual risks and untested areas. The audit does not replace the device acceptance checklist below or provide independent certification.

## 0.6.1 default SMS setup recovery — 2026-09-23

- The default-app request now waits for Android's activity result, checks the actual held role, and displays a persistent outcome. Cancellation and launch failure leave a user-triggered link to Android's default-app settings. The link opens the standard settings screen; it does not grant permissions or change the role itself.
- Role checks now use RoleManager.isRoleHeld for this app rather than querying the default SMS package without the visibility declaration documented by Android. All existing sender and receiver role gates continue to use this shared check. This is a correction to the implementation, not proof of the precise failure on the user's phone.
- Returning from Android settings refreshes setup indicators immediately. Indicators also update when an edited conversation draft is retained. Setup actions do not generate, approve or send messages.
- assembleDebug, testDebugUnitTest and lintDebug passed: 39 unit tests, zero failures/errors, zero lint errors and five existing warnings.
- tools/ui-setup.cjs passed with an explicitly simulated native bridge: pending request, unsuccessful selection, cancellation, chooser launch error, successful selection, direct Settings action, role gain/loss on resume, preserved edited draft, no setup-triggered draft saves or sends, and no mobile overflow. The first test run incorrectly counted the user's deliberate draft edit as a setup save; its assertion was corrected to inspect only calls after setup began, then the suite passed.
- tools/ui-safety.cjs passed: rejected approval, text escaping, cancellation and delay bounds. The new mobile setup screen was visually inspected.
- APK signature, update version, asset consistency, unchanged permission list and 16 KB ZIP alignment are checked during packaging. Pairing and profiles retain the same package/signature and storage schema; queued sends pause on update under the existing recovery behavior.
- No relay change, AI request or real SMS was made. Native role-dialog lifecycle and real carrier sending still require verification on the Pixel. This update cannot override Android restricted settings or device-management policy.

September 23, 2026. Automated suites send no real SMS and mock OpenAI. A separate live deployment check successfully generated one synthetic OpenAI draft after the user configured the key privately. Real Pixel, carrier, background scheduling, Android Keystore and broader OpenAI output-quality checks remain outstanding.

## Version 0.6.0 — per-person radio form

- APK version 0.6.0, code 7, built successfully. 39 Android unit checks passed, including profile context with no optional note, preservation of maximum-length existing notes, per-person tone resolution and all automatic-drafting opt-in combinations. Lint has zero errors and the same five warnings as the baseline. APK signature matches 0.5.1 and 16 KB ZIP alignment passes. No new permissions.
- Schema 4 adds relationship type and saved tone to each conversation. `tools/check-profile-migration.py` exercised the production SQLite statements for fresh setup and upgrades from schemas 1, 2 and 3, preserving existing notes, samples, opt-ins, drafts and queued replies. Android's actual update lifecycle still needs device testing.
- All six browser scripts passed after updating selectors for the nested optional-samples disclosure. Checks cover real radio inputs, saved choices, blank optional dynamics, isolation between people, persistence across reloads, keeping edited drafts, HTML escaping, opt-in-only automatic drafting and the 412-pixel layout. The mobile form screenshot was inspected.
- All 14 mocked-provider server tests passed. The provider context allows 1,600 characters so the selected relationship label can accompany an existing 1,500-character dynamics note without truncation. The editable note remains limited to 1,500 characters.
- Only selected automatic profiles start background drafts; When I ask and Off do not automatically generate even a local quick suggestion. Foreground manual tone choices remain available, while automatic drafts use the saved per-person tone or the app default. Existing profile revision guards reject results generated against changed preferences.
- Railway deployment 1daa2005-209f-4bc8-9e26-b6b6e26f74c0 completed successfully on September 23, 2026. Existing API credentials and phone pairing were retained.
- A live fictional coworker request with Professional selected and both optional dynamics/samples blank returned HTTP 200: “What time is the shift on Friday?” in 1,841 ms. This verifies drafting from radio choices without requiring free text; it is not a quality or latency guarantee.
- No real SMS was sent. The new installer still needs to be applied and checked on the Pixel, including automatic drafting and carrier acceptance tests below.

## Version 0.5.1 — AI intuition

- Built APK version 0.5.1, code 6; 35 Android unit checks passed. Lint passed with the same five warnings listed below. APK signature matches 0.5.0; 16 KB ZIP alignment passed. The package contains the new tone controls. No permission or database-schema change.
- 14 Node tests passed, including a check that the new tone survives request validation, reaches the provider and adds adaptive guidance only when selected. Fixed tones keep their previous provider instructions.
- UI cloud and smoke scripts passed with the updated assets. They check that Use AI intuition reaches the mocked native test bridge, saves as the default, preserves edited text, and fits the phone layout. Other browser suites below describe the 0.5.0 baseline and were not rerun for this narrow update.
- Railway deployment 47020047-253d-4af7-b45b-9fbcc4c3e12e completed successfully on September 23, 2026. Only the five allowlisted server/build files were uploaded. Existing variables and phone pairing were retained.
- Two fictional inputs using Use AI intuition returned HTTP 200 from the deployed gpt-6-sol service: a disappointed friend received “I’m sorry. That really sucks. Want to talk about it?” in 2,142 ms; a driving-test celebration received “ahh nice!! congrats on passing!” in 1,771 ms. These are spot checks of appropriate adaptation, not a general quality or latency guarantee. No real conversations or SMS were used.
- The user reports successful pairing and OpenAI test replies with 0.5.0. The 0.5.1 update still needs installation and testing on their Pixel; carrier and background-send acceptance checks remain outstanding.

## Version 0.5.0 baseline

## Completed Android checks

- `assembleDebug` and `testDebugUnitTest`: passed, 35 tests (13 send-policy, 11 local-prompt, 4 Nano-test-prompt, 7 cloud-context selection tests).
- `lintDebug`: zero errors, five non-blocking warnings: pinned Gradle/dependency versions, ARM-only target, intentionally enabled packaged WebView JavaScript and required telephony hardware.
- APK version 0.5.0, code 5. Signature verification passed with the same development certificate as 0.4.0; installable as an update rather than a separate app.
- 16 KB APK ZIP alignment passed. Native libraries are unchanged from the previously checked build.
- Packaged permissions include INTERNET and ACCESS_NETWORK_STATE for native HTTPS and network-constrained draft jobs. Cleartext is disabled. WebView network loads and remote navigation remain blocked.
- Desktop SQLite exercised the actual production migration statements from schema 1 and schema 2 to schema 3: preserve existing notes, drafts and jobs, default new cloud switches off, separate samples per chat, increment revisions, clear profile content. Android upgrade lifecycle still requires a device.

## Service checks

The 0.5.0 baseline had 13 passing Node tests. OpenAI responses are mocked explicitly; these are not inference tests.

- Authentication protects health and generation; HTTP rejects browser origins, unknown routes, wrong content types, malformed JSON and oversized bodies.
- Only allowlisted context fields reach the provider request. Request-controlled models, tool definitions, contact names and numbers are stripped. Incoming text remains data; the fixed system instructions cannot be replaced through request fields.
- `store:false`, fixed provider endpoint, no redirects or tool capabilities; service credentials never appear in responses or raw error messages.
- Concurrent duplicate IDs trigger one call; changed content with the same ID is rejected. Per-minute, daily and concurrency limits behave as documented for one running instance.
- Refusals, incomplete outputs and empty/oversized answers are errors. Completed assistant text is extracted even when reasoning items precede it.
- A loopback HTTP test checks the real server process without an API key, with no sensitive request content logged.

## Browser checks

All six Playwright scripts passed in Chrome with a 412-pixel phone layout, plus desktop smoke checks. Screenshots are interface previews with synthetic data.

- Edits survive tone/timer changes and navigation; user-edited drafts are not automatically replaced. Search, preferences, multimedia empty state and mobile layout work.
- Rejected acceptance leaves the queue unchanged. Approved exact text is queued, cancellation prevents the simulated send, and delays beyond seven days are rejected.
- HTML-like messages, notes, samples and model output render as text, not executable markup.
- Relationship context and samples stay separate by person, persist in the browser demo and clear after saving. Unsaved profile edits block redrafting. Saving a profile does not change drafts or send queues.
- OpenAI and person-level automation start off. Turning OpenAI off disables that person's automatic switch. OpenAI is the test screen's initial provider.
- An explicit native-bridge mock checks connection save, status messaging, OpenAI test inputs, errors and disconnect. It verifies the test flow never invokes any send, approval, conversation-generation or save-draft action.
- The browser preview disables real AI testing and never fabricates a test result. The Nano bridge mock verifies request isolation, escaped output, errors, three-result comparison and clearing.

## Native policy and context selection

The send-policy suite covers approval, due time, cancellation, duplicate dispatch state, changed conversations, revoked permissions, lost default role, late alarms, recipient/body limits and invalid delays. These are policy tests, not carrier tests.

The cloud-context suite checks conversation isolation, exclusion of failed/unsent messages, eight-turn and six-style-example limits, opt-out of automatic style samples, independent explicit profile samples, incoming-message requirement, rejection of overlong incoming text, bounded Unicode-safe clipping of older examples and no profile carry-over between requests. Prompt tests do not prove model obedience or human-like wording.

## Required device acceptance checks

Use a second phone and ordinary test messages. Do not rely on this build as the primary messenger until these pass:

1. Deploy the service behind HTTPS, add the API key privately, and pair the phone. Before changing the default SMS app, open Try AI → OpenAI and generate with synthetic input. Check that wrong tokens, revoked keys, unavailable billing, offline mode and invalid certificates fail safely. Test optional Nano separately. Verify it reports real output or an availability error and creates no SMS, draft or queued send. Compare different context/tone, try repeated requests and clear the session.
2. Default-role selection is offered. Grant SMS, phone-state, contacts (optional), notifications and exact-alarm access. Verify existing SMS history.
3. Verify the selected SIM. Test incoming SMS, non-Latin text and multipart SMS; outgoing SMS must appear in Android message storage and show a carrier outcome.
4. Accept an edited draft and verify the exact recipient/content arrives once. Reject the confirmation and verify nothing is queued or sent.
5. Schedule, lock the screen, wait and verify the send. Cancel before its deadline and verify no send.
6. While queued, receive a newer message from that conversation; verify the old reply pauses. An enabled automatic profile may prepare and schedule a fresh reply only for the new incoming text.
7. Reboot, change the clock, force stop, revoke permissions/default role and remove the selected SIM. No stale queued reply should send without the documented checks; force-stopped apps cannot execute alarms until Android permits them again.
8. Simulate airplane mode/no signal and carrier failure. Confirm a visible failed or unknown state and no automatic retry; multipart failure can mean partial delivery.
9. Check Gemini Nano status, prepare a model if available, generate while foreground, and verify failure is actionable if unsupported. Compare Natural drafts with your own recent sent texts, try matching on and off, and test a conversation with no sent examples. Review for invented facts or commitments. Verify short local Smart Reply suggestions with an English incoming SMS. Test that Nano stops being usable while backgrounded.
10. Install as an update and verify existing drafts/messages survive. Add different relationship notes and samples to two chats; restart and check they stay separate. Redraft with and without each note, confirm boundaries influence tone, and check the note itself is not disclosed. Clear a note and verify later drafts omit it.
11. Verify incoming MMS download, text and image rendering with this carrier. RCS, outgoing MMS and group replies are out of scope. Switch back to a full messenger if those are required.

12. New profiles must start Off, and upgrading must preserve existing opt-ins and notes. In Reply setup choose Prepare automatically for one person, When I ask for another, and Off for a third. Receive texts from each; only the automatic profile may start generation. Confirm its saved tone and relationship are used, even if the app default differs. Toggle global automatic drafts off and verify no new background requests. Save profile changes, disconnect, or receive a new message while a request is in progress; stale results must not overwrite current text or start a timer.
13. Generate a cloud draft, edit it, approve it, and verify the approved text is sent once after the chosen delay. No test reply, request failure, profile save or connection action may queue a send. With auto-send off, model output must not queue a send. Only a fresh real-conversation OpenAI draft with explicit Send after timer enabled may schedule without individual review.
14. Verify the final approval buttons work normally with no overlay. With a benign test overlay fully or partially covering that dialog, touches must not accept a reply. Dismiss the overlay and verify normal use resumes. Recheck optional local AI generation/model download after the dependency updates.

15. For Send after timer, verify no existing draft schedules just from enabling the mode. Receive an ordinary test SMS from the selected person; verify a new OpenAI draft starts the saved delay without a per-message confirmation. Check original text, suggested reply, sound and countdown with the phone locked. Expand the notification, open it and verify the exact text. Disable lock-screen previews and check redaction without an extra sound.
16. Cancel from the notification and verify no send; repeat by editing the automatic reply. Disable the person's mode, change profile settings, turn global automation off and disconnect while separate automatic timers are pending; each affected timer must pause. Verify manually confirmed jobs remain separate. Block notifications before and during generation; no automatic send may be armed and the saved draft must remain visible. Close/reopen the app during a timer without revoking authorization and confirm status is preserved; reboot must pause it. Test interruption during alert posting, airplane mode, stale drafts, late alarms and one-/two-SIM cases.
17. Use at least two profiles with different automatic delays, tones and notes. Confirm one person's preferences never apply to another. Cycle through all themes, restart, and check persistence. Open the keyboard in a long conversation and verify the composer stays visible. Confirm themes/profile navigation cannot start sending.

18. In one contact's profile choose Send instantly and save. Confirm the existing displayed draft is not sent. Receive a new ordinary test SMS and verify one fresh AI reply sends after generation, without review/countdown, while other contacts retain their modes. Test with precise-timer access disabled: instant mode should work, delayed mode should pause. Check the original/reply notification and carrier result, then global off, per-person off, disconnect, new-message/profile changes during generation, offline failures and duplicate callbacks. Never infer delivery from carrier acceptance alone.

## Remaining security and deployment verification

Verify the deployed host terminates HTTPS correctly, keeps API secrets private, uses one replica, disables unwanted sleeping and does not log request bodies or authorization headers. Confirm key/token rotation and OpenAI project billing controls. In-memory service limits reset with restarts and are not account spending caps. The app has not undergone an independent security audit.

## Railway deployment checks

The connected user signed into the Railway CLI. Deployment fb8ba57c-9d9a-473b-8fab-8cc562db142b completed successfully on September 23, 2026. Only five allowlisted server/build files were uploaded; the APK, samples, private phone-token files and signing key were excluded. Service is configured with one replica and sleeping disabled.

At https://reply-pilot-api-production.up.railway.app, standard HTTPS certificate validation passed. Public /status returned 200; /health without a phone token returned 401. The initial authenticated /health check returned 200 and ready:false before the key was configured.

After the user added OPENAI_API_KEY privately, deployment edfb3b00-e3f9-4d4f-a67d-f5445df9aee6 succeeded. Service configuration was inspected for variable names only; the key was not read. Authenticated /health returned 200 and ready:true. One POST /draft with invented friend/coffee context returned 200, engine OpenAI · gpt-6-sol, and “Maybe, what time were you thinking?” in 2,623 ms. This establishes a successful live provider request with the configured model at that time; it is not a latency guarantee or a broader quality evaluation. No real conversation, contact information or SMS was involved. The user subsequently reports successful pairing and test replies. Device/carrier acceptance tests remain outstanding.
